# Dig Defend — Build Milestones

Source: game design doc, exported to `gdd.txt` in this repo. Live version: https://docs.google.com/document/d/1uSE6rXA2rIwvqYDbySOI4nr2OXErxfMbViEymtUqWaQ/edit (re-export with `curl -sL "https://docs.google.com/document/d/1uSE6rXA2rIwvqYDbySOI4nr2OXErxfMbViEymtUqWaQ/export?format=txt" -o gdd.txt`). Reference projects: `~/md` (dragons) and `~/square` (lights).

Each milestone should be testable and playable before moving to the next. Milestones are numbered `M<n>` in build order within a phase. Phases are roughly sequential, but later polish/meta phases can interleave once core gameplay (Phase 6) is playable.

Rules:
- Never mark a milestone complete or commit code without explicit approval.
- Every gameplay number lives in a `GameData` data-only module (checked into git for this project, unlike the reference games).
- Every clonable asset (rocks, monsters, weapons, tools, posts, defenses) lives in one central `ReplicatedStorage` location.
- Core loop first, art and polish later. A cube enemy is a fine enemy until Phase 9.
- When copying UI or systems from the reference games, restyle to this project's UI standards (fonts, capitalization, centering, sparse exclamation points).

Open design questions (sections of the GDD that are still empty headings, to be filled before their phase begins): Backpack, Boots, Posts, Defenses, Traps, Camp Level, Wave Cycle, Monsters, Random Events, Adjusting for Player Counts, Revives, Spectate Mode, Menus, Audio.

---

# Upcoming

## Phase 2: Player Lifecycle, Persistence, and Lobby Shell

## Phase 3: Queues and Teleporting

## Phase 4: Mine Generation and Mining

## Phase 5: Camp, Defense, and Placement

## Phase 6: Enemies and the Core Loop

**M30 — Wave data and difficulty scaling**
Per-map, per-difficulty wave composition in GameData. Enemy HP/damage multipliers by difficulty (difficulty lives in the waves, decided 2026-09-12), plus wave count and enemy HP multipliers by party count from `PartyScaling` (the main party lever). Uranium rescue target multiplier by party count. Wave progression escalating each night. Needs the empty "Wave Cycle" section filled first.

**M31 — Win condition and rescue helicopter**
Uranium target per map/difficulty. Reaching it enables calling the rescue helicopter; final climactic wave spawns as it arrives; boarding the helicopter wins the run. Completion recorded in PlayerData (only if the player had completed the prerequisite themselves) to unlock maps and difficulties in the Create Party menu.

**M32 — Death, revives, and spectate**
Player death handling, revive mechanic, spectate mode for dead players, all-dead loss. Needs the empty "Revives" and "Spectate Mode" sections filled first.

**M33 — Player-count scaling and return to lobby**
Wave and resource adjustments for 1 to 6 players. Run-end summary screen with Cash milestones earned, then teleport back to the lobby. Needs the "Adjusting for Different Player Counts" section filled first.

**M34 — First full playtest pass**
Play Deadman's Canyon Normal solo and with a group, start to finish. Fix blockers, tune the first-pass numbers. This is the "core game is playable" gate.

## Phase 7: Meta Progression and Lobby Economy

**M35 — Store config and Store Menu**
Single `StoreConfig` GameData file driving Featured, Weapons, Posts, Placements, and Currency sections. Three-panel Store Menu with vertical scrolling inventory, item panel, and buy flow for Cash and Robux. Insufficient Cash routes to the smallest sufficient currency pack. Owned non-repeatable items hidden.

**M36 — 3D Store**
Showcase models in `Workspace.Store.Items` with `StoreItem` attributes and proximity prompts that open the purchase flow or the store menu section.

**M37 — Loadout Menu**
Owned/unowned/equipped states, equip/unequip/buy actions, slot replacement rules, five-slot loadout display. Loadout carried into the gameplay place via PlayerData.

**M38 — Cash milestones**
In-run milestone table awarding Cash. (Player level, XP, the level-10 bonus loadout slot, and level-gated pads were removed from the design on 2026-09-10; pads gate on completions only.)

**M39 — Daily Rewards**
Daily Rewards Menu ported from lights with configurable day count and rewards, restyled to this project's UI standards.

**M40 — Dev products and Robux purchase handling**
`ProcessReceipt` for currency packs and Robux store items, idempotent granting, purchase logging.

## Phase 8: Second Map and Content Breadth

**M41 — Map system**
Map selection from join data drives camp layout, mine config, and wave data. Camp structure placement authored per map.

**M42 — Frostbite Pass**
Second camp with more exposure and its own mine and wave data. Weather placeholder.

**M43 — Difficulty tiers for both maps**
Hard and Nightmare tuning for both maps. Map and difficulty unlock chain verified end to end.

**M44 — Launch content**
Full launch roster of weapons, post weapons, defensive units, traps, and mining tool tiers with data and placeholder models.

**M45 — Random events**
Mid-run random events framework and a first few events. Needs the empty "Random Events" section filled first.

## Phase 9: Art, Audio, and Feel

**M46 — Rock, mining, and refinery feel**
Final rock textures, crack overlays, hit particles, ore pop, refinery suck-in/grind/fly-to-HUD sequence with balance bar shake.

**M47 — Enemies, weapons, and camp art**
Replace placeholder enemy, weapon, post, defense, trap, and structure models with final assets from the central asset location.

**M48 — Lobby art and helicopters**
Lobby environment, helicopter models and takeoff animation, 3D store showcases, loading screen art.

**M49 — Audio**
Sound effects across mining, combat, UI, and camp; ambient and music per place; volume options. Needs the empty "Audio" section filled first.

**M50 — UI polish pass**
Every menu checked against the UI standards from M10. Mobile layout verification on small screens.

## Phase 10: Performance, Stability, and Launch Prep

**M51 — Low-end device performance**
Profile on a 2 GB phone and low bandwidth. Tune streaming radius, rock pooling, enemy shadow update rates, and message sizes.

**M52 — Robustness**
Teleport failure paths, mid-run disconnects and rejoins, server shutdown handling, data save flush under load, error reporting.

**M53 — Analytics and telemetry**
Run funnel events (queue, run start, day reached, win/loss, purchases) for post-launch balancing.

**M54 — Balance pass and soft launch**
Full balance pass on both maps and three difficulties using playtest and telemetry data. Launch checklist: place permissions, group gating, dev products live, event area sign in place.

---

# Done

**M1 — Toolchain and two-place project** ✅
Finished 2026-09-10. Foreman/Rojo/Wally/luau-lsp toolchain mirroring `~/square`. Two Rojo projects (`lobby.project.json` on port 34872, `gameplay.project.json` on port 34873, each pinned to its place id) sharing `src/shared`, `src/server`, `src/client`, with per-place `src/lobby` and `src/gameplay` folders. `analysis.project.json` feeds `scripts/analyze.sh`. Both places synced into Studio and played with boot lines and no errors. `.gitignore`, `README.md`, and `CLAUDE.md` seeded; non-adopted reference standards listed at the bottom of `CLAUDE.md`.

**M2 — Core shared libraries and coding standards** ✅
Finished 2026-09-10. Ported `Enums` (with GDD-seeded enums), `utils`, `Validate`, `Tags`, `Attributes`, `NetEventNames`, and an extended `Constants`, all generic and `--!strict`. GameData lives in `src/server/GameData` (server-only, in git), seeded with `Control/Difficulty`; LSP stubs cover it. Conventions and the object/item recommendation (skip `Item`, port the definition registry at M25 as `EntityType`) are in `CLAUDE.md`. Flagged: GDD says both "Brutal" and "Nightmare" for the top difficulty.

**M3 — Test suite runnable from Studio** ✅
Finished 2026-09-10. jest-roblox 3.10.0 via Wally dev-dependencies, committed in `DevPackages/`. `src/tests/TestRunner.luau` (square's Edit-mode workarounds) and `src/tests/shared/*.spec.luau` are mounted at `ServerScriptService/Tests` in both places and in `analysis.project.json`. Four specs (`utils`, `Validate`, `Enums`, `Constants`), 56 tests, green from the Studio Command Bar; Claude runs them through the Studio MCP and reads the Output. Run line documented in `CLAUDE.md` and `README.md`.

**M4 — Single RemoteEvent message bus** ✅
Finished 2026-09-10. `NetManager` creates the one `RemoteEvent` in code and runs the receive pipeline (rate limit → Ready check → known name → `Validate.isTableShape` → pcall handler) with throttled rejection warnings; per-player `TokenBucket` (unit tested) tuned by `GameData/Control/Network`. `ClientNetManager` mirrors it with loud unhandled-name warnings. `GameController` assembles handlers from `getNetHandlers()` owners. Feedback channel kept inside the one-RemoteEvent rule: `PlayerFeedbackManager:give()` sends `NotifyPlayerFeedback`; `ClientPlayerFeedbackManager` also owns the `GivePlayerFeedbackLocal` BindableEvent and prints until the M9 ScreenGui listens. Verified in Play: good/bad/unknown messages in both directions. Analyzer now reports only colon-method self-inference errors (ignored by decision); consider filtering that class in `scripts/analyze.sh`.

**M5 — Dev tools and group-gated access** ✅
Finished 2026-09-10. Iris 2.5.1 via Wally (`Packages/`, committed). Server `DevTools` handles `TryDevTool` against the `DevToolCommands` allowlist and re-checks admin per message; admin = rank ≥ 254 in group 571203718 (group-page roles, not Creator Hub collaborators), stamped as the `IsAdmin` Player attribute. `ClientDevTools` Iris panel (RightShift / triple-tap) with Players, Icon Framing, test feedback, and confirm-gated reset. Reset contract mechanism (`PLAYER_READY_SEQUENCE` / `PLAYER_TEARDOWN_SEQUENCE` in `GameController`) established, empty until M6. Icon capture pipeline ported with Windows replacements: `docs/icon-capture.md`, `scripts/capture_studio_window.ps1` (PrintWindow), `scripts/process_icon.sh` (ImageMagick 7, installed via winget). Verified in Play: non-admin refused; admin path handled valid/unknown/bad-args/reset commands. 8 suites / 75 tests. Lesson: stop `rojo serve` before `wally install` (Rojo 7.7 crashes).

**M6 — Player lifecycle and PlayerData** ✅
Finished 2026-09-10. ProfileStore vendored in `ServerLibs/` (mounted `Server/Libs`, excluded from analysis). `GameController.playerDatas` is the single lifecycle table (LOADING → READY → LEAVING → GONE); `getPlayerData` returns nil unless READY and fences NetManager and DevTools. `PlayerData` owns the live record and profile; the save shape, defaults, and repair rules are the pure shared `PlayerSaveSchema` (cash, ownedUnlocks set, loadout by `Enums.LoadoutSlot` name with WEAPON/POST/DEFENSE_1..3, completions keyed by `CompletionKeys` "MAP/DIFFICULTY", dailyReward, sessionMeta). XP/level dropped from the design. Tuning in `GameData/Control/Player` (store `DigDefendPlayerData_v1`, starting Cash 0, 30 s load timeout). Reset dev tool now wipes the save and overwrites the live profile; `setCash` dev command added. Verified against the real DataStore (Studio API access enabled): Cash survived a rejoin, sessions and playtime accumulate, reset wrote defaults; 3-client Test-tab stress run saved all three fake players on leave with no session locks left and no errors. 10 suites / 89 tests.

**M7 — Client data folders** ✅
Finished 2026-09-10. `SplendidReplicationManager` builds the `SplendidGames` ScreenGui at Ready (first entry of the ready sequence): `GameMetadata` (ServerStartTime, SchemaVersion, PlaceKind), `AllPlayers` (UserId, DisplayName, IsReady per Ready player; balances and flags private by decision), `CurrentPlayer` (Cash, IsAdmin, five loadout slot attributes, daily reward progress, `OwnedUnlocks` and `Completions` marker folders). Attribute sets come from shared `PlayerPublicSchema` (unit tested). `replicate*` methods are the only write path after a PlayerData mutation; reset rewrites the tree in place. `ClientDataManager` (awaitReady, typed reads, attribute/marker/AllPlayers listeners) gates `main.client`; dev tools read IsAdmin and the Players window from the tree. Verified: live Cash listener, reset in place, and a 3-client Test-tab run where every client's AllPlayers showed all three entries with only public fields and dropped a leaving player. 11 suites / 94 tests.

**M8 — Lobby map skeleton and spawn** ✅
Finished 2026-09-10. Studio-authored (via MCP) Workspace folders `SpawnZone` (platform, four SpawnLocations, sign), `HelicopterPads` (six `Pad<n>` models with `PadIndex` attribute, `JoinCollision`, `ExitSpawn`, `HelicopterSpawn`, `CameraPositionPart`, `GuiPart.PadGui`; the designer rebuilt Pad1's slab as a `Pad` model and authored the four-label `PadGui`, then pads 2–6 were cloned from it), `Store` (shell + empty `Items`), `EventArea` (fenced, "Coming Soon" sign), `HoldingArea` (sealed room at y=-120 with six `Spawns`). `ReplicatedStorage.GameAssets` created with a placeholder `Helicopter` template (`PlayerSpawns` x6). Third-person camera via StarterPlayer. Lobby-only `LobbyMapManager` (`src/lobby/server`, started by the lobby `main.server`) validates the whole tree at startup and warns per missing piece; names in `Constants`. Decisions recorded on M11 (pad config in GameData, `PadIndex` only on the model) and M12 (billboard colors). Place published by the designer.

**M11 — Party config and pad state** ✅
Finished 2026-09-11 (built out of order, before M9/M10, because the pad redesign landed first; nothing in it depends on them). Redesigned 2026-09-11 (see `docs/helicopter-pad-party-spec.md` and the GDD "Helicopter Pads – Party Creation/Teleports" section): pads are no longer pre-assigned to a map and difficulty. All six pads are identical; the first player onto an idle pad becomes the host and configures map, difficulty, party size, and Public/Friends Only visibility (Dead Rails' Create Party flow, no join-by-code). Pad models stay as built in M8 (`PadIndex` attribute, `HelicopterSpawn`, `CameraPositionPart`, `GuiPart.PadGui`, `JoinCollision`, `ExitSpawn`; helicopter template in `GameAssets` with `PlayerSpawns`). `GameData/Pads/Party` holds `CREATION_TIMEOUT`, `BOARDING_TIMEOUT`, `CAPACITY_TIME`, `MAX_PARTY_SIZE`, `DEFAULT_MAX_PLAYERS`, `DEFAULT_VISIBILITY`, and the ordered map and difficulty lists with requirements as `Enums.Map` / `Enums.Difficulty` references, validated at startup (unique ids, requirements resolve, timeouts positive, defaults in range). `Enums.PadState` (`IDLE`, `CONFIGURING`, `BOARDING`, `DEPARTING`) and `Enums.PartyVisibility` (`PUBLIC`, `FRIENDS_ONLY`). Server `PartyManager` owns one Lua party record per pad (state, host, settings, ordered member list, timer end time) and replicates state, host, map, difficulty, max players, visibility, member count, and departure server-time as attributes on the pad model. The pad billboard (`GuiPart.PadGui`, re-authored for the four states: "Open", "Setting up…", configuration + count + friends-only icon + countdown, "Departing") renders from those attributes; countdowns are computed client-side from the replicated end time, never ticked over the network. Pure unlock and validation rules unit tested. The `Timer` label was added to every `PadGui` in Studio (PlayerCount shrunk to make room); `LobbyMapManager` requires all five labels. Playtested by driving pad attributes by hand; all four billboard states verified.

**M9 — Arrival loading screen and feedback text** ✅
Finished 2026-09-11. Lucky Squares pattern: `src/first/loading.client.luau` (ReplicatedFirst, no requires) shows the Studio-authored `Loading` ScreenGui on frame one and removes the default screen; `ClientLoadingScreenManager` takes over via the `LoadingHandoff` attribute, fills the bar in stages from `main.client` (save, world, assets via `PreloadAsync` on `GameAssets`, systems), shows retry/slow-connection captions from `LoadStatus`, holds a minimum visible time, and tweens off. `PlayerFeedback` ScreenGui (`FeedbackBox.Message`) driven by `ClientPlayerFeedbackManager`; a newer message replaces the current one. Both GUIs are built by `scripts/studio/build_*.luau`, which must be replayed in the gameplay place when it is first opened (M14). Title image is a text placeholder until art exists; colours and fonts provisional until M10. Layout standard (scale + aspect/size/text-size constraints, offset only for hairlines) decided here and recorded in CLAUDE.md; both GUIs rebuilt to it. GUI standard (goal: identical intended appearance on every device type) recorded in CLAUDE.md; Device Emulator pass deferred to M10 when all three screens are restyled together.

**M10 — Lobby HUD** ✅
Finished 2026-09-11. `HUD` ScreenGui (built by `scripts/studio/build_hud_gui.luau`): `CashWallet` (tap opens the store currency tab at M35), `LoadoutStrip` with five slot frames carrying the `LoadoutSlot` attribute, `MenuButtons` (Store, Loadout, Daily Rewards; "Coming soon" feedback until their menus exist). Lobby `ClientHudManager` flashes Cash green/red with the `CoinsAdded`/`SaleSuccess` sounds before updating, renders slots from the loadout attributes, and slides all three groups off screen on overlay begin. Common `ClientOverlayManager` (Lucky Squares: one overlay, blur, click-catching backdrop, frozen character, FOV nudge, deferred while loading) and `ClientSoundManager` (plays designer-authored `AudioPlayer`s under `SoundService.Sfx`/`Music`; names in shared `SoundNames`; warns once for empty assets). Shared `UiStyle` (Poppins Bold ALL CAPS titles, Montserrat body, palette, timings, text bounds) recorded in CLAUDE.md; Loading and PlayerFeedback rebuilt to it. Dev tools gained "Toggle test menu (overlay)" as the stand-in menu until M35. Sound asset ids are empty until the designer can upload; source files in `audio/`. SoundService structure must be mirrored in the gameplay place (M14). Designer confirmed the look and the Device Emulator pass on 2026-09-11. Still owed: sound asset ids once the designer has upload permission (source files stay in `audio/` as the archive of record).

**M12 — Host, configure, join, and leave** ✅
Finished 2026-09-11. Touching `JoinCollision` on an idle pad seats the toucher in the pad's helicopter (cloned from `GameAssets` at startup, one per pad), moves the camera to `CameraPositionPart`, and opens the `CreateParty` menu (map and difficulty carousels with locks and requirement text, players stepper, Friends Only toggle, "Cancelling in N seconds", Create disabled while locked, Cancel). `TryCreateParty` carries the whole selection, validated against the host's completions server-side; on success the pad boards, the boarding timer starts (20 s, decided 2026-09-11), and the `Queue` menu opens in host mode (Launch) or member mode (Exit). Join checks, full-party timer clamp (never rises), host promotion, seat-leave and disconnect handling are in `PartyManager`; pure rules and the config codec in `PartyRules` (unit tested). Place managers register net handlers and teardown through `GameController:registerPlaceManager`; the party config reaches clients as one JSON attribute on `GameMetadata`. Party menus hide the pad billboards and lock jumping; the seat is left only via Exit or Cancel. The rotor (`Rotar.Rotar`, blades welded) spins client-side while Boarding or Departing (`ClientHelicopterManager`). Launch is stubbed until M13 (timer expiry or Launch press empties the pad). Studio fixes found in playtest: `JoinCollision` parts were unanchored and fell out of the world at Play (now anchored, validator warns), `CameraPositionPart`s faced away from the helicopter, helicopter seats are real `Seat`s kept `Disabled` so the engine never auto-seats a passer-by. Multi-player paths (join checks, friends-only, promotion) are implemented but were only exercised solo.

**M13 — Launch and teleport** ✅
Finished 2026-09-11. Launch on host Launch press, boarding timeout, or capacity countdown (a party of one fills at Create and launches after `CAPACITY_TIME_SEC`). `PartyManager:runLaunch`: Departing, `TAKEOFF_SEC` wait, members unseated and moved to random `HoldingArea.Spawns`, `ReserveServer` + one `TeleportAsync` with the member list (the deprecated party call is not used), retried `TELEPORT_RETRY_COUNT` times; final failure respawns each member in `SpawnZone` with "Teleport failed" and restores their menu and camera; stragglers after `TELEPORT_SETTLE_SEC` get the same; the launched helicopter is renamed `DepartedHelicopter` (destroyed after its lifetime) and a fresh one cloned, pad to Idle. Join data (`PartyRules.buildJoinData`) carries map, difficulty, actual player count, host, pad index, and member ids; the gameplay place's `ArrivalGateManager` kicks arrivals that are not listed (warn-only in Studio). Client (`ClientHelicopterManager`, every client): departed helicopter climbs 100 studs then cruises 1000 studs along its facing and is removed; rotor resolved lazily against streaming and the helicopter template set to Atomic streaming after inconsistent spins. Party members see the Queue menu fade while the camera stays on the pad, then after 2 s the `Loading` ScreenGui in departure mode ("Flying to <map>...", reused for now, may change). Gameplay place synced with Rojo for the first time and published; the designer confirmed a live lobby-to-gameplay teleport. Studio-only verification covered the failure path (Studio teleports always 403).

**M14 — Gameplay place arrival** ✅
Finished 2026-09-11. Gameplay `RunManager` records the first valid join data as the run (map, difficulty, player count, host, member ids) and replicates map/difficulty/count/host as `GameMetadata` attributes; `ArrivalManager` (replaces the M13 gate) kicks arrivals not in the party (warn-only in Studio), places characters on the designer's `Camp.HelicopterPad.Spawns` parts 1-6 in arrival order, spawns the camp helicopter at `HelicopterSpawn` with the rotor turning, and flies it off 3 s after every expected member is Ready (or 45 s after the first arrival); tuning in `GameData/Run/Arrival`. `GameplayMapManager` validates the camp tree. Helicopters became one system for both places: common server `HelicopterManager` clones, tags (`Tags.HELICOPTER`), and sets `Enums.HelicopterPhase`; common `ClientHelicopterManager` animates every tagged model; the lobby maps pad states to phases. `GameController:registerPlaceManager` now also admits `onPlayerReady`; `DevTools:registerPlaceCommand` and `ClientDevTools:addMenuSection` let places add commands, used by the gameplay "Return to lobby" dev tool (`RunDevTools` / `ClientRunDevTools`) for round trips. Dev windows free the mouse (first person locks it). First person via `StarterPlayer.CameraMode` (in `gameplay.project.json` and set in Studio). The departure screen is handed to `TeleportService:SetTeleportGui` and the ReplicatedFirst bootstrap keeps the arriving copy, so no Roblox loading screen shows and the bar (empty during the flight) fills once on arrival. Gameplay place: Loading and PlayerFeedback GUIs replayed, SoundService `Sfx`/`Music` mirrored, published. Designer confirmed the live round trip: launch, arrive on spawn 1 in first person, helicopter leaves, return to lobby.

**M15 — Rock definitions and weight kernel** ✅
Finished 2026-09-12. `Enums.RockName` (19 rocks; keys are the `ReplicatedStorage/Rocks` template names, public names match the GDD hand-placed strings) so M16 stores one integer per grid cell. `GameData/Rocks/RockDefinitions` (filler order, resource tier lists, HP/yield/cluster radius from the GDD tables) and `GameData/Rocks/Weights` (`SPREAD = 1.5`, 1.0–4.0 authoring range); difficulty values and `HARDNESS_SHIFT` stay in `GameData/Control/Difficulty`. Shared `RockRules` (pure, tuning passed in): `weights` kernel, `clampCenter` returning a was-clamped flag for named warnings, `resolveHardnessWeights`, `resolveTierCounts` (largest-remainder rounding, epsilon tie to the lower tier, always sums to the authored count), seeded `pickWeightedIndex`, `effectiveHardness`, `effectiveVeinCount` (round half up), and `validateRockTable` naming the offending rock. Spec covers every GDD hardness row, the 6/18/6/0 and Nightmare examples, clamping, spread tuning notes, and validator rejections; it mirrors the rock table like `PartyRules.spec` because GameData cannot load under jest. Nothing visible in Studio; playable = suite green (152 tests) and analyzer clean.

**M16 — Mine generator (data only)** ✅
Finished 2026-09-12. Shared `MineGenerator` (pure; config assembled from `GameData/Rocks`, `GameData/Control/Difficulty`, `GameData/Mines/Generation`): the eleven GDD pipeline steps in order, a flat grid of `Enums.RockName` integers (0 = air) in mine-local depth/height/width, resolved chambers, and a report (seed, grid size, per-chamber diggable volume, share within pickaxe reach, requested vs placed ore including hand-placed rock, HP, cumulative uranium). Validation returns fatal and warning lists naming parameter and chamber; `preview` gives expected ore and HP from the file alone; `formatReport` prints one line per chamber. Seeded: nil draws and reports a seed; each chamber uses its own sub-seed so editing one never reshuffles another. Mine files in `GameData/Mines` (`DeadmansCanyon` = GDD worked example, `FrostbitePass` placeholder copy, `Registry` by `Enums.Map`) plus `Enums.MineFacing`. Design decisions made here: difficulty lives in the waves, so `HARDNESS_SHIFT` is 0 and a separate `VEIN_DIVISOR` (1.0 / 1.2 / 1.4) makes ore scarcer; uranium is exempt from it, authored 2 / 4 / 8 / 14 against `uraniumTarget = 20` so the counter moves from chamber 1 while the last chamber stays required (fatal rule), placed only within pickaxe reach of the floor and never overlapping itself, and re-checked on the built grid against the six-player multiplier (1.25 placeholder until `PartyScaling` at M18). Party scaling never touches rock HP. 200 seeds of the real data generate in ~6 ms each with exactly 28 uranium. Spec covers grid size, inheritance, sealing, mouth/corridors/gates, hand-placed rock, seed reproducibility and per-chamber isolation, clearance, report, vein divisor, uranium reach, every fatal and warn rule, bedrock overrides, and preview parity. Nothing visible in Studio; playable = suite green (183 tests) and analyzer clean.

**M17 — Mine replication and client rendering** ✅
Finished 2026-09-12. Maps are Folders under `ServerStorage/Maps` named by `Enums.Map` key (decided over Models so a click selects one part, and over a place per map to keep one codebase); `MapManager` clones the run's map into Workspace as `Camp` at run start (an unsaved map folder left in Workspace is used with a warning) and pastes an optional terrain snapshot; `scripts/studio/map_tools.luau` loads a map for editing and saves it back (`TerrainBounds` opts a map into terrain); `scripts/studio/setup_maps.luau` migrated the camp and created the `FROSTBITE_PASS` placeholder copy. `RunManager` starts the run on first arrival (Studio falls back to `GameData/Run/StudioDefaults`) and run-start subscribers run in order: load map, place helicopter and spawns (`ArrivalManager`), generate mine (`MineManager`). Mine origin and facing come from the map's `Mine.Entrance` part; `GameplayMapManager:checkCamp` validates each map and the rock templates. Server holds the grid and per-block damage in Lua (`MineManager:applyDamage` ready for M18), never parts. Replication over the single RemoteEvent: `NotifyMineHeader` then `NotifyMineChunk` buffers, versioned; pure `MineGridCodec` run-length encodes, and filler travels as one placeholder the client re-rolls from the seed with the shared kernel (`MineGenerator.collapseFiller` / `expandFiller`, round trip unit tested), which took the worked example from 45 KB to 14 KB in 4 chunks. Client `ClientMineManager` rebuilds the grid (36 ms), precomputes exposed cells (1,410 for the whole mine; out-of-bounds counts as air only in front so the mountain's hidden top and sides cost nothing), and renders those within `STREAM_RADIUS_BLOCKS` from a per-rock part pool (864 parts inside chamber 1, under 4 ms per refresh); pure `MineSpace` maps cells to world; `ClientTicker` ported from Lucky Squares. 19 placeholder rock cubes under `GameAssets/Rocks` (`scripts/studio/build_rock_templates.luau`). Dev tools: "Regenerate mine" with optional seed and "Start run as" map/difficulty, plus live bytes/parts/pool/refresh numbers in the panel and as attributes on the client `MineBlocks` folder. Tuning in `GameData/Mines/Streaming`. Designer confirmed the walk-in on 2026-09-12. Still owed: a real 2 GB phone measurement (Studio cannot stand in), and placeholder mountain geometry in each map folder since only the face with the mouth renders.

**M18 — Custom tool bar, pick axe, and Mining Mode** ✅
Finished 2026-09-12. `Enums.ToolKind`; gameplay `ToolManager` holds each player's tool slots, equipped tool, and pick axe level as run state (granted at Ready, kept across a dev-tool reset, cleared on leave) and answers `TryEquipTool`; `SplendidReplicationManager` gained `setPublicAttribute` / `setCurrentPlayerAttribute` for per-player run state that survives tree rewrites, so the equipped tool is public (`AllPlayers`) and slots/level private. `ClientToolBarManager` drives the `ToolBar` ScreenGui (`build_tool_bar_gui.luau`; slots cloned from `SlotTemplate`, tap toggles, keys 1-9, bumpers cycle, slides away under menus); `ClientToolModelManager` welds `GameAssets/Tools/<ToolKind>` (`build_tool_templates.luau`, placeholder pick) to every player's right hand from the public attribute. Mining Mode: `ClientMiningManager` enables the `MiningHud` ScreenGui (`build_mining_hud_gui.luau`; reticle + `TargetLabel`) while the pick axe is held, raycasts the camera centre against rendered blocks within reach, highlights the target with name and HP (max hp per rock travels in the mine header), swings on mouse/touch/RT with hold-repeat at `HIT_INTERVAL_SEC`, and sends `TryHitRock` with the cell. Server `MiningManager` checks equipped, diggable, reach + tolerance from the player's root, and a per-player cooldown, applies `MineManager:applyDamage`, and broadcasts `NotifyRockDamaged` / `NotifyRockBroken`; clients tint the block at 60%/30% (placeholder crack look kept off the part so recycling and re-streaming keep it), clear the cell and re-expose neighbours on break, and play placeholder particle bursts plus `RockHit` / `RockBreak` (AudioPlayers authored, assets empty). Shared `MiningRules` (damage by level, reach, cooldown, crack stage, slot codec, party yield lookup) unit tested; data in `GameData/Mining/PickAxe` (5/12/25/50, 0.45 s swing, 12-stud reach, 6-stud tolerance, 0.35 s cooldown) and `GameData/Control/PartyScaling` (Iron/Gold per-player yield 1.0-1.75 by party size; first consumer M19). Verified through the real remote: equip, held model, two hits break Shale, neighbour exposed, 5 fast hits throttled to 1, far cell refused. Designer confirmed 2026-09-12. Known placeholders: no swing animation; on touch a camera drag over a block also swings.

**M19 — Ore drops, backpack, and pickup** ✅
Finished 2026-09-12. Gameplay `BackpackManager` holds contents, level, and capacity (ore units, `GameData/Mining/Backpack` CAPACITY_BY_LEVEL 50/100/200/400) as run state replicated as one private CurrentPlayer attribute per raw resource plus level and capacity; `add` returns (added, overflow) and sends `NotifyOreCollected` to the collector for the pop. `MiningManager:awardOre` gives the breaker a broken resource rock's yield, Iron and Gold multiplied by `PartyScaling` for the current Ready count (dawn sampling is M28) and rounded half up, and hands overflow to `OrePickupManager`: pickups are Lua state plus one invisible anchored shadow part each (`Tags.ORE_PICKUP`, `PickupResource` / `PickupAmount` attributes) scattered 1.5 studs from the block, polled 4x/s against Ready players with room within 5 studs (partial collection allowed), expired after 120 s, capped at 300 oldest-first. Client `ClientBackpackManager` drives the `GameplayHud` backpack button (`build_gameplay_hud_gui.luau`: icon, taken/total, red numbers and "Backpack full" label) shown while the pick axe is held, bumps it and plays `OrePickup` (AudioPlayer authored, asset empty) on collection, and opens the `Backpack` contents menu (`build_backpack_menu_gui.luau`; rows cloned from `RowTemplate`, only ores carried, overlay via `ClientOverlayManager`). `ClientOrePickupManager` draws a bobbing, turning cube per tagged pickup in the tier-1 rock template's colour on one shared Heartbeat. Shared `BackpackRules` (capacity by level, party yield, add with overflow, takeAll for M20, attribute per resource) unit tested. Dev tools: Fill backpack / Empty backpack in the mining section. Verified through the real remote: Crystal 3 break gave 24, fill to 50/50 showed the full state, an Iron 1 break while full dropped a 5-ore pickup with its visual, and walking over it after emptying collected all 5. Designer confirmed 2026-09-12. Any player may collect any pickup (decided).

**M20 — Refinery** ✅
Finished 2026-09-14. Gameplay `RefineryManager` binds the camp's `Refinery.Prompt` ProximityPrompt at run start (text and range from `GameData/Refinery/Refinery`), hears its trigger server-side, takes the whole backpack, disables the prompt for the 2 s grind, then credits: Iron Bars and Gold Bars as private CurrentPlayer attributes, Diamonds into the shared Camp Level pool and Uranium into the shared rescue count as GameMetadata (`CampLevel`, `CampDiamonds`, `CampDiamondsRequired`, `UraniumTotal`, `UraniumTarget`). Shared `RefineryRules` (1:1 rates, XP-style level rollover with carry from `GameData/Camp/Level` placeholder thresholds 20/80/250, party-scaled uranium target from the new `PartyScaling.URANIUM_TARGET_MULTIPLIER_BY_PLAYER_COUNT`) unit tested. `GameplayMapManager` validates `Camp.Refinery.Prompt` + prompt. Client `ClientRefineryManager` renders the `GameplayHud` balance rows (Iron, Gold, Uranium "total / target", green at target) and the Camp Level title + bar, and plays the placeholder sequence: up to six coloured discs per resource fly from the backpack button into the Refinery's screen position on `NotifyRefineStarted` (RefineryGrind), then from the Refinery to each row on `NotifyRefineCompleted` (RefineryPayout), bumping the row and revealing the value as the last lands. Dev tools: Fill backpack now spreads all four ores; Reset refinery. Also this session: `map_tools.luau` `preview_mine` / `clear_preview` draw the generated mine as labelled boxes in Edit mode (server deletes a leftover `MinePreview` at run start); ore blocks play `ResourceHit` on hit via a `rockKind` table in the mine header; CLAUDE.md "Working with the user". Verified through the real prompt: fill (12/12/12/14) → 12 Iron, 12 Gold, bar 12/20, Uranium 14/20; second refine → Camp Level 2 with 4/80 and Uranium 28/20 green. Sound assets for RefineryGrind/RefineryPayout still to be authored. Designer confirmed 2026-09-14.

**M21 — Mining Supplies Store and mining tools** ✅
Finished 2026-09-15. `Camp.MiningStore` holds one `<Item>Stand` Model per `Enums.StoreItem` (designer-built PickAxeStand as the template, cloned and normalised by `scripts/studio/build_mining_store_stands.luau`): a Base slab with a SurfaceGui (NameLabel, PriceBox with centred icon + auto-width price, red "Camp Level N required" only while unmet) and an ItemSpawn where the client spins the next level's display model. Server `MiningStoreManager` creates and binds each stand's ProximityPrompt (1 s hold), works out the offer with shared `StoreRules` (level N+1 needs Camp Level N; maxed; consumables never capped; catalogue codec, unit tested), spends Gold Bars via `RefineryManager`, and applies the upgrade to `ToolManager` (pick axe), `BootsManager` (Humanoid WalkSpeed 16-24), `BackpackManager`, or `JetPackManager`, or grants consumables; the catalogue is published once as GameMetadata JSON and `ClientMiningStoreManager` draws every stand per viewer. Prices in `GameData/Store/MiningSupplies`. Tools: held models are real Roblox Tools built by `ToolManager` from `GameAssets/Tools` (one pick axe per level, TORCH, DYNAMITE; Roblox hotbar off), with the `toolanim` Slash on every swing; consumable counts travel as a JSON attribute and show as an xN badge, the slot leaving at zero. Torch (`TorchManager` / `ClientTorchManager`): placed on the aimed surface within reach, shadow part + client stick/flame/shadowless light, drops as a tool pickup when its block clears (`MineManager:subscribeCellCleared`, `OrePickupManager:dropTool`). Dynamite (`DynamiteManager` / `ClientDynamiteManager`): thrown stick simulated by the thrower's client (the server has no blocks), 3 s fuse, 3x3x3 blast via `MineManager:clearCell`, all ore to the ground, 60-stud sanity radius. Jet pack (`JetPackManager` / `ClientJetPackManager`, `JetPackRules` unit tested): double-tap-and-hold Jump or the touch button, server-owned tank (1.5 s + 0.5 s per level, 12 studs/s climb, refills anywhere at 0.5/s, locks until full after running dry), client-applied lift, worn `GameAssets/Wearables/JET_PACK` with flame and light, green/red fuel bar under the always-visible backpack button in a right-edge HUD stack. 3D audio: `SpatialSoundManager` clones authored Sfx players onto objects with an AudioEmitter (dynamite fuse and bang, jet burn, helicopter loop from spawn); `ClientSoundManager` keeps an AudioListener on the camera; Fail plays on backpack-full and tank-dry transitions; all Sfx assets authored. Also this milestone: mine `floorDrop` per zone so the mine goes down as well as up (Deadmans Canyon 2/4/6/8; preview updated), Iron/Gold cluster radii shrunk and zones 1-2 evened to 22/20 and 22/22, depth-blended mine darkness to 100 studs (`ClientMineLightingManager`), pickups settle to the floor, off-screen pickups fly in from the screen edge, rock HP shown as a name + health bar (no numbers), `MiningStore` may be a Folder or Model. Dev tools: Add gold, Set camp level, Give torches and dynamite. Studio: the MCP thread can no longer `require` (build scripts run through a loadstring loader; jest runs from the command bar). Designer confirmed 2026-09-15. Still owed: real art for every placeholder model and icon (M46), the lobby's Helicopter AudioPlayer, and a Torch/Dynamite decision on non-mine surfaces (currently placed where the client says, within reach).

**M22 — Camp Level unlocks** ✅
Finished 2026-09-16 (the small version, agreed with the designer: the bar, rollover, and store gating already existed from M20/M21). GDD "Camp Level" section finished first: the unlock table (Pick Axe, Boots, Backpack, Jet Pack level N+1 needs Camp Level N; Posts tier 1 is the base and tier N needs level N; the post branch fork and capstone need level 4; weapon in-run tiers Gold only; roster unlocks are lobby Cash), and the designer's S9–S12 defense notes saved into the GDD as a block for M23–M27. `GameData/Camp/Level` now holds `MINING_UPGRADE_REQUIRED_CAMP_LEVEL_BY_LEVEL`, `POST_TIER_REQUIRED_CAMP_LEVEL_BY_TIER`, and `POST_CAPSTONE_REQUIRED_CAMP_LEVEL` beside the diamond thresholds; `StoreRules` reads the gate from the track's `requiredCampLevelByLevel` (copied into the published catalogue by `MiningStoreManager`, so the client still never requires GameData) instead of a formula, an unmentioned level is never gated, and the codec round-trips the table (unit tested, 252 tests green). Level up: `RefineryManager` broadcasts `NotifyCampLevelUp` once per payout for the level reached; `ClientRefineryManager` shows "Camp upgraded to level n" in the new blue top-centre `AnnouncementBox` of the PlayerFeedback GUI (`ClientPlayerFeedbackManager:announceLocal`, `UiStyle.ANNOUNCEMENT_COLOR`) and plays the designer-authored `CampLevelUp` Sfx (gameplay place only, `audio/Sfx/levelup.mp3`). Dev tools: Add diamonds, through the real payout path. Nothing enforces the Posts rules until M25. Designer confirmed 2026-09-16.

**M23 — Camp structures with HP** ✅
Finished 2026-09-17. Gameplay `StructureManager` holds HP for the 6 walls, the gate, and the Refinery in Lua, one shadow part each (`Tags.STRUCTURE`, `Structure*` attributes). The map's `Walls/Wall1..6`, `Posts/Post1..6`, and `Gate` are placement markers swapped for `GameAssets/Structures` templates (`WALL_n`, `GATE_n`, `POST_n`, `WALL_RUBBLE`, `GATE_RUBBLE`; `scripts/studio/build_structure_templates.luau`, create-only so designer edits survive) at the marker's pivot; the server numbers each post's sign. Decided in playtest: the gate, walls, and post look share one fortification level (1 to 3, HP 200/400/800 walls, 300/600/1200 gate, share of HP kept on upgrade); everyone starts with a Hammer whose equip is Build Mode (HP bars, reticle, highlight, "4 [Iron icon] for 10 HP"); repair works like mining, one validated `TryRepairStructure` swing per 0.45 s adding 10 HP for 4 Iron at 17 studs reach (`ClientHoldInput` carries the press, hold, and touch-dwell rules); rubble stays rubble until fully rebuilt (`REBUILD_STAND_SHARE`); the upgrade is crowd-funded at a `Workbench` by the gate, 10 Iron per prompt press into a shared pool (150 / 400), level N gated by Camp Level N, upgrading by itself with the Camp Level style announcement; the Refinery's HP level is the Camp Level and `subscribeRefineryDestroyed` is the M28 loss hook; the gate stands open by day and shut by night (`setNight`, dev tool until M28). Pure rules in shared `StructureRules` (unit tested), numbers in `GameData/Camp/Structures` and `Camp/Level`. Dev tools: damage/destroy a structure, add Iron, set fortification level, day/night. Sounds: `HammerHit` is a pitched `RockHit` stand-in. Deadman's Canyon saved back under `ServerStorage/Maps`.

**M24 — Weapons** ✅
Finished 2026-09-17. Everyone starts the run with their loadout weapon, or the default Rifle, as the second tool bar slot (`WeaponManager`; `ToolManager` holds the carried `Enums.WeaponKind` and builds the held Tool from `GameAssets/Tools/WEAPON_<KEY>`). Numbers in `GameData/Weapons/Handheld`: every weapon is hitscan with a magazine and unlimited reserve; the Rifle is a lever-action to match its art (5 rounds, 0.6 s a shot, 30 damage, 2.5 s reload, decided in play). A shot is `TryFireWeapon { origin, direction }`: the server checks equipped, loaded, cooldown, and that the ray starts at the head, raycasts the world, and tests the ray against `DamageableManager`'s hit boxes with pure box maths (shared `WeaponRules`, unit tested), then broadcasts `NotifyWeaponFired`. `DamageableManager` is the shadow-part registry of everything a weapon can hurt, only the dev-tool Target Dummy until M29; players and structures never register. The mine's rocks exist only on clients, so they stop a tracer but not the server's ray (revisit at M29). FPS feel, built as a system for M25 posts to reuse: `ClientViewModelManager` draws a camera-locked copy of the weapon (sway read from camera rotation, bob, recoil kick, lowered reload pose) and hides the real Tool for its owner; `AimManager` / `ClientAimManager` replicate look pitch as the public `AimPitchDegrees` (4 Hz, on change, quarter-cost message) and lean other players' waist, shoulder, and neck by it (pure `AimRules`, unit tested); every weapon template carries a `Muzzle` attachment with a `MuzzleFlash` light (0.125 s) and `MuzzleSmoke` emitter (0.1 s). `ClientWeaponManager` runs Weapon Mode through `ClientHoldInput` and the touch action button, with a predicted ammo count, the `WeaponHud` ammo readout (tap, R, or gamepad X reloads), and a flying tracer streak; `ClientImpactManager` pools the impact dust and the 10 s bullet hole decals (`GameAssets/Effects/BulletHole`), which watch their surface so none is left floating. Sounds are per weapon, never shared (`SoundNames.WeaponSfxByKindName`, a spec fails for a weapon without a full set): `RifleFire` is a client-side 3D one-shot with an explicit falloff curve (`ClientSoundManager:playSfxAt`), plus `RifleReload`, `RifleEmpty`, `RifleHit`. Also: the mining reticle is now lent by borrower name so Build Mode and Weapon Mode never fight over it. Designer's Rifle art stays untouched in `GameAssets/Weapons`; `build_tool_templates.luau` builds the held copy (scaled, Handle at the lever, welded, Muzzle measured by raycast). Dev tools: spawn / clear target dummies. 286 tests green. Not in this milestone: the other three weapons, in-run weapon tiers, the lobby loadout UI.

**M25 — Defensive Posts** ✅
Finished 2026-09-18. GDD "Posts" section written first from the designer's write-up. Gameplay `PostManager` owns claims, weapons, manning, aim, upgrades, heat, reloads, and rockets as Lua state on the post slots `StructureManager` fills (it exposes `getPostSlots` and fires `subscribePostModelsFilled` on every swap, so a fortification tier keeps claims). Claiming is the sign's "Claim Post" prompt (1 s hold, 10 Gold, 3 studs below the sign), shown only to a player without a post on a post nobody owns; the sign reads UNCLAIMED with a "CLAIM FOR [Gold icon] 10" row (hidden per client once they own a post), then the owner's headshot, name, and weapon. The weapon is cloned from `GameAssets/PostWeapons/<KIND>` onto the post's Spawn (the post's own Spawn turns transparent), a bench from `GameAssets/Structures/POST_WORKBENCH` onto the template's `WorkbenchSpawn` marker. Mounting is the weapon's Use prompt: the character is seated on `CharacterSpawn` with two IKControls on the handles, the tool put away, the HUD (balances, backpack, jet pack, tool bar) hidden, the camera Scriptable at `CameraLocation` looking down the barrel at the kind's FOV (Machine Gun 30, Rocket Launcher 40), the mouse locked every frame, and the weapon's own reticle image shown (`Icons.POST_WEAPON_RETICLE`). Aiming: the base turns and the gun tilts at the same slow Turn Speed (22 / 12 degrees per second, decided in play), the client sending `TrySetPostAim` at 10 Hz and the server refusing any step the turn speed could not make (pure `PostRules`, unit tested), echoing on the weapon model; every client turns and tilts the model from that, the gunner a frame ahead; a 2D `Post_Turn` whir fades in and out with the gunner's own movement. Firing is `TryFirePostWeapon` with the muzzle ray, validated against the server's own muzzle: the Machine Gun is hitscan with an overheat (3 s of fire fills it, 3 s to cool, locked until cool, Fail sound, OVERHEATED label); the Rocket Launcher flies a server-stepped rocket (`GameAssets/Projectiles/Rocket`, 120 studs/s, 12 stud splash, authored `Trail` on its TailAttachment and `GameAssets/Effects/RocketBlast` burst with `BurstCount` per emitter) and its 6 s reload is its fire rate, shortened by Fire Rate upgrades (RELOADING label). A status bar above the exit hint shows heat (green to red) or the reload (red filling to green). Exit: Spacebar, gamepad A or Cross (B too), or a touch Exit button standing in for the hidden jump button; any menu opening also exits. Upgrades: the owner-only bench prompt opens the `PostUpgrade` overlay (Damage, Fire Rate, Turn Speed, Range; four pips each; 15 / 30 / 60 / 120 Gold; level N needs Camp Level N, CAMP LEVEL N REQUIRED under the pips; refused buys play Fail with the reason). Sounds authored as `Post_<Weapon>_<Action>` AudioPlayers (Fire and Hit each, plus Flight and Explode for the rocket). Claims release on disconnect or dev reset. The lobby loadout store is later, so every claim spawns the Machine Gun (default); dev tools set the loadout post, claim / mount / release by number. Studio: `build_post_hud_gui`, `build_post_upgrade_menu_gui`, `build_post_workbench_template`. 311 tests green. Not in this milestone: the branch fork and capstone, the lobby loadout UI, Mortar and Flak.

**M26 — Defensive Zones, Traps, and Towers** ✅
Finished 2026-09-21. GDD "Defenses" and "Traps" sections written first from the designer's write-up (Defensive Units renamed Towers). Gameplay `PlacementManager` owns every placed Tower and Trap as Lua state with one shadow part each (`Tags.PLACEMENT`, `Placement*` attributes, the Tower's Upgrade/Sell prompt and the Trap's Sell prompt on it); the map's `Camp.DefensiveZones` parts are numbered and tagged at run start and their grids are pure maths both sides run from the part (shared `PlacementRules`, unit tested: grid, footprints, occupancy, prices, sell value, loadout resolution, codecs). A build is `TryBuildPlacement { placementKind, typeKind, zoneIndex, column, row }`, checked for the Defenses tool, the loadout or the Trap inventory, the 5-Tower limit, free cells, distance, and Iron. Decided in play (2026-09-21): a `Defenses` tool in the tool bar (no held model) is Build Mode, replacing the Hammer as the way in because a locked first-person cursor cannot click a bar; the Hammer's mode is now Repair Mode. In Build Mode the tool bar hides and the `BuildBar` takes its place with one numbered slot per Tower and Trap, styled like a tool bar slot with a kind badge; one entry is always selected (entry 1 on entering); LMB / RT builds, RMB / B exits, the touch bar has Build and Exit buttons; the preview stands 20 studs ahead and only exists inside a zone, its parts going red where it cannot go ("Cannot place here", "Not enough space"; "Go to the Defensive Zone to place Defenses" outside); a Tower counter ("2 / 5") and an always-on-top DEFENSIVE ZONE billboard show while the mode is on; the movement mode is pinned to KeyboardMouse in both places so right click never walks. Towers: Sentry Gun by default, five stat rows per kind in `GameData/Defenses/Towers`, all 4 by 4 cells; the `TowerMenu` overlay (tower defense layout) sells at half of everything spent, upgrades for Iron, and cycles the target priority. Traps: bought at the mirrored `DefenseStore` stands for Iron (three kinds carried), consumed on placement, sold back at half from their prompt; `GameData/Defenses/Traps`. `DefenseStoreManager` / `ClientDefenseStoreManager` mirror the Mining Store. Also this milestone: every GUI now lives in StarterGui (OverlayBackdrop, DevTestMenu, the flying icon template; rule in CLAUDE.md), tool bar slots and the HUD backpack and jet pack show pictures from `Icons`, and the lobby got the current PlayerFeedback GUI. Studio scripts: `build_tower_templates`, `build_trap_templates`, `build_placement_billboard`, `build_defensive_zone_billboard`, `build_build_bar_gui`, `build_tower_menu_gui`, `build_defense_store_stands`, `build_defensive_zones`, `build_overlay_backdrop_gui`, `build_dev_test_menu_gui`. Dev tools: loadout towers, grant traps, damage / clear placements. 334 tests green. Not in this milestone: Tower and Trap behaviour (M27), the lobby loadout UI, the `PlacementBuild` / `PlacementSell` sounds.

**M27 — Defensive unit and trap behavior** ✅
Finished 2026-09-22. GDD "Tower Types" and "Trap Types" rewritten first from the designer's roster (Tiger Teeth renamed Wall). Targets: `DamageableManager` grew into the registry enemies will use at M29: an airborne flag, status effects through pure `StatusRules` (the strongest slow wins, a stun is a 100% slow, a burn is damage per second and a fresh burn restarts the clock), and an optional straight walk stepped every Heartbeat that stops against a barrier and attacks it. Walking and flying dummies are dev-tool buttons beside the target dummy. Towers: `TowerBehaviorManager` steps every Tower with pure `TowerRules` (target by Closest / Furthest / Strongest / Weakest, cones, chain jumps, turret yaw); each kind's fixed numbers and its four Tower Menu stat rows are `BEHAVIOR` and `STAT_ROWS` blocks in `GameData/Defenses/Towers`, with new `Enums.TowerStat` rows (Flame Length, Slow Rate, Freeze Length, Chain Count) so a Frost Tower never shows a Damage of 0. Sentry, AA (air only), and Sniper are single shots; Flamethrower and Frost breathe a cone for the flame length and rest; the Tesla Coil zaps and chains with a brief stun. Range is measured from the footprint's edge (decided in play: an 18-stud Range reached 10 studs past a 16-stud Tower), and a Tower preview carries a ring of its reach on the ground. Traps: `TrapBehaviorManager` with pure `TrapRules`; Wall and Decoy are barriers (the Decoy's shadow carries a draws-attacks flag for M29), spikes pop under a crosser and run an Up / Recharging clock published as `TrapState`, goo slows, a mine blows once, a bear trap hurts and stuns one crosser once. Towers are repaired with the Hammer exactly like walls (`TryRepairTower`, anyone, same chunk and price; Repair Mode aims at Tower bodies and shows the wall billboard); Traps never are. Client: turrets ease round, flames and spike states follow attributes, tracers, bolts, and trap pops are placeholder neon parts; `NotifyTowerFired` / `NotifyTrapTriggered`. Studio: the WALL template rebuilt as a solid block, the store stand renamed. 359 tests green. Not in this milestone: sounds, art, the Flamethrower burn (the designer's roster lists only Damage), enemy routing around barriers (M29).

**M28 — Day/night cycle and run state machine** ✅
Finished 2026-09-22. GDD "Wave Cycle" and "Adjusting for Different Player Counts" written first. `RunCycleManager` (gameplay server) is the state machine: `Enums.RunState` Waiting (the crew landing: morning look, clock still), Day (5 minutes), Night (until cleared), Won (stub for M31), Lost (the Refinery destroyed, or every Ready player down, checked once a second). Day 1 begins `PRE_DAY_SEC` (10 s) after the helicopter leaves, which `ArrivalManager` now publishes (`subscribeLaunched`: everyone the lobby sent is Ready, or the arrival timeout). A night ends by the dev tool, by `NIGHT_PLACEHOLDER_SEC` after the 90 s spawn window (M29 sets it to 0), and later by the last enemy dying; a cleared night sets the next day's start `LIGHTING_DAWN_TRANSITION_SEC` ahead so the sky blends first. The run state, day, phase start and end times (server time), and the sampled crew size are `GameMetadata`, so every client draws the same clock; the gate follows the night flag through `StructureManager:setNight`. Crew size (pure `RunCycleRules`) is sampled at run start from the party that boarded and at every dawn from the Ready count, only ever going down; ore yield and the uranium target read it. Day-start line ("Day 3. Crew of 4. Ore 1.45x, Uranium target 1.15x") and the loss go to the announcement box. Client: the `DayClock` ScreenGui (`build_day_clock_gui`, a CanvasGroup top centre under the announcement box, which moved down to make room) with the phase circle (day / dusk / night tints until the art lands, `Icons.UI.CLOCK_*`), the bar emptying right to left from the published times, and "Day N"; it fades out for the night and back at dawn. The designer's `DayNightNumber` card shows "DAY  N" / "NIGHT  N" for 3 s once the dawn or dusk blend ends (no card for day 1) with the new `Rooster` / `NightBell` sounds. Sky: shared `LightingPresets` per map hold four looks read from Studio (Day Start 8:00, Day End 17:30, Night Start 19:12 with the light off just past the engine's sun-to-moon handover, which cannot be blended, Night End 20:00); every frame the client sets the blend `RunCycleRules.skyStage` gives (day start to day end over the day, a 1 s dusk into night start, night start to night end over the spawn window then held, a 1 s dawn back to day start), the clock turning forward between hours; nothing tweens. Dev tools: Dusk now (runs the day down over `FORCED_DUSK_SEC`), Clear the night, Lose the run. Also: `ClientDataManager`'s ready wait carries an explicit timeout so the five-second infinite-yield warning is gone. 375 tests green. Not in this milestone: the clock pictures, the end-of-run flow back to the lobby (M33), enemies ending a night (M29).

**M29 — Enemy framework** ✅
Finished 2026-09-23. GDD "Setting Up Waves" (Wave Area, Creating Waves, Wave Spawn Locations) and "Monsters" written first from the designer's roster, with the group-generation rule for M30 decided. Performance first (researched): monsters are never server models or shadows. `MonsterManager` keeps each as a Lua row stepped at 10 Hz, registered in `DamageableManager` without a shadow (which gained shadowless entries, a per-frame shared target list, HP share and burning queries), so every weapon, post, Tower, and Trap works on them unchanged. Positions reach clients as 11-byte records (pure `MonsterSnapshotCodec`, unit tested) on one code-created `UnreliableRemoteEvent` (the single-RemoteEvent exception, recorded in CLAUDE.md), per player by distance: moving and within 80 studs every tick, within 200 every other, else and when standing still every fourth; spawns, deaths, projectiles, and blasts are batched on the reliable event. `ClientMonsterManager` pools Humanoid-free rigs and Proxy blocks (never parented in and out), interpolates 0.15 s behind with short extrapolation, and moves everything in one `BulkMoveTo`: the 40 nearest animated, the rest to 250 studs in a held pose, beyond that the Proxy; nothing is ever hidden. Measured on a desktop: 300 monsters about 2.6 ms of server step, 8 to 18 KB/s, 60 fps; 500 about 2.2 to 3 ms and 19 KB/s (phone still to check). Paths: `MonsterPathManager` lays one 4-stud grid over the map's new `Camp.WaveAreas` and `Camp.CampArea` parts (`build_monster_areas.luau`, with numbered `Camp.MonsterSpawns`), scans ground and fixed obstacles from just above each area (a post or roof is never taken for floor), and runs pure `FlowField` Dijkstra from the Refinery: walls, the shut gate, Towers, and blocking Traps are barriers a ground monster pays to cross, recomputed on change, so a blocked crowd breaks through the cheapest wall; air uses a static field. Crowds walk as a band: each monster its own lane off the route, steering to the farthest point it can see 8 cells ahead, separation 4 studs, and a stalled crowd stands still (no jitter, no bandwidth). Data: all 18 monsters in `GameData/Monsters/Roster` (tiers are separate kinds; Bomber melee, Launcher range 200, Titan II 1000 damage), `DetectionRange` 20 (the highest-priority target detected is walked to, only over clear ground, then attacked within AttackRange), `AttackInterval`, `FlightHeight` 36 for the Flyers, default R6 clips (Flyers walk in the fall pose, all attack with 129967390); tuning in `GameData/Monsters/Behavior`; per-map waves in `GameData/Waves` (one test wave). Each monster has its own template `GameAssets/Monsters/<KEY>` whose invisible Proxy is the hit box, far drawing, and feet (`build_monster_templates.luau` converts a rig, `seed_monster_templates.luau` seeds new kinds); the designer added bombs and wings, welded to the Torso. `MonsterWaveManager` runs the night: groups by time, remaining groups move up when the field is empty, the last monster dying clears the night (`NIGHT_PLACEHOLDER_SEC` now 0); players are held at 1 HP until M32. HP bars: one bar on the damaged monster under the reticle, any distance; the Refinery's bar is always up and a wall or the gate shows its bar while damaged. Dev tools: spawn any monster, kill all, stress test with live server step, KB/s, and draw cost, path grid view. 406 tests green. Not in this milestone: shot lag compensation, LeashDistance (unused: chases stay on the grid), air melee dives, balance (the Launcher one-shots tier-1 walls, two Flyers take the Refinery in about 10 s), the wave generator and scaling (M30).
