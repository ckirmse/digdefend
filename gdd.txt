Tab 1
…
Let’s build a Roblox game. Connect to Roblox Studio using MCP.
Overview


* The apocalypse has arrived and the world’s most valuable resource is Uranium 
* Apocalyptic Mining Co has decided to send its crews to remote and dangerous mining locations to extract Uranium
* For each run, a group of 1-6 players are dropped off at one of these remote mining camps
* Every mining camp has a mine, a processing machine, a segmented wall, 6 mannable posts, a gate, a mining supplies building, a defense building, and a helicopter landing pad.
* The team’s ultimate goal is to collect x amount of Uranium, hidden ever deeper in the mine
* The challenge is that every night a wave of enemies descends on the camp, bent on destroying it, with each wave getting progressively harder
* Players must use the ores and materials they find mining to build and upgrade their camp defenses to keep the enemy hordes at bay at night, so they can mine in the day
* To win, players will need to extract the required amount of Uranium and survive long enough to be airlifted out by the company’s rescue helicopter
* Players lose by dying or having their processing machine destroyed
* In addition to in run currency, players receive Cash for reaching certain milestones during each run, which they can use outside the run to unlock new weapons, post upgrades, and defensive placements
* As players level up their skills, they’ll be able to embark on more challenging missions in different locations 
Place Structure
* The game will have two separate places: a lobby place and a gameplay place. 
* Everyone will join the lobby when they launch the game (start place). This is where players group up, buy upgrades/unlocks/consumables/classes and check event status. 
* The gameplay server is where gameplay takes place and can only be reached via teleports from the lobby
* Players form parties at helicopter pads inside the lobby, which teleport them as a group to a fresh gameplay server where they will play the main game
* Only players in a specific queue should be able to access the specific gameplay server they’re teleported to. Friends and other players should not be able to join an existing gameplay server. 


The Lobby 


* The lobby will be the Apocalyptic Mining Co. headquarters, a high security base in the middle of a post-apocalyptic wasteland
* The lobby will have a few map locations. Each zone will have its own folder under workspace (Workspace.SpawnZone, Workspace.HelicopterPads, etc.) 
   * SpawnZone - a central area in the map where players spawn in 
   * 6 Helicopter Pads (party creation – teleports to the gameplay server) 
   * 3D Store (shows featured items, gives secondary access to store menu)
   * Event Area - an area reserved for future events 
   * HoldingArea - a room under the map where teleporting player characters can be placed during the teleport process. 
* Players will have access to different menus/guis:
   * HUD
   * Store Menu
   * Loadout Menu
   * Create Party Menu
   * Queue Menu
   * Daily Rewards Menu  
   * Player Feedback Text
   * Arrival Loading Screen
   * Departure Loading Screen 
Lobby Map Areas
SpawnZone
* This will be a central area in the lobby that contains all SpawnLocations. Players will spawn here when they join or if they have teleport issues. 
Helicopter Pads – Party Creation/Teleports
* Helicopter Pads teleport parties of players to a new, private instance of the gameplay place
* All 6 pads are identical. A pad is not tied to a map or difficulty. The first player onto an idle pad becomes the party host and configures the run (map, difficulty, party size, visibility). This is modelled on Dead Rails' Create Party flow, without join-by-code. Adding a map or difficulty later means adding it to the party config, not building a pad.
* Every Helicopter Pad has a Helicopter sitting on it when the server initializes (cloned from ReplicatedStorage.GameAssets.Helicopter onto HelicopterSpawn). Its rotor spins (client-side) from Create until the pad returns to Idle. The Helicopter sound rides on every helicopter as a 3D emitter and each client plays it from the first turn of the rotor until the blades are still again or the model is destroyed (decided 2026-09-16), so the sound and the blades always agree.
* Every Helicopter Pad has a billboard gui at its entrance with three labels (decided 2026-09-11):
   * Status: Idle "Waiting for players...", Configuring "Player configuring", Boarding "Starts in: X" (seconds until departure), Departing "Departing"
   * Private: hidden while Idle or Configuring; once a party exists it shows "Public" or "Private" from the party config
   * Players: always shows current players / party size (an idle pad reads 0 / MaxPartySize)
* Pad states. Each pad is in exactly one state, held in server Lua and replicated to clients as an attribute on the pad model so the billboard and menus can render it:
   * Idle: empty, no host. Touching JoinCollision makes you the host and moves the pad to Configuring.
   * Configuring: the host is on the Create Party Menu. Nobody else can join; touching gives the feedback "Pad is being set up".
   * Boarding: configured and accepting players who pass the join checks.
   * Departing: teleport in progress. Nobody can join; touching gives "Pad is departing".
   * Transitions: Idle → (touch) → Configuring → (Create) → Boarding → (launch) → Departing → (reset) → Idle. Configuring returns to Idle on creation timeout or if the host leaves. Boarding returns to Idle if nobody is left.
* Becoming host
   * A player touches JoinCollision on an Idle pad. The server rejects them with "You're already in a party" if they are in a party on any pad.
   * The server sets the pad to Configuring with this player as host and starts the CreationTimer at CreationTimeout.
   * The host's character is seated in the helicopter at the first PlayerSpawn and their camera is moved to CameraPositionPart. While in a party, jumping is disabled so the seat can only be left through the menu (Exit or Cancel); the pad billboards are hidden while a party menu is open.
   * The host's client opens the Create Party Menu.
   * If two players touch an Idle pad at the same time, the first processed becomes host and the second gets "Pad is being set up". Never two hosts.
* Configuring
   * The Create Party Menu shows a countdown. Changing a setting does not reset it: CreationTimeout is a hard limit on how long a pad can be held without committing.
   * If the countdown reaches zero, the host presses Cancel, or the host leaves the game: the host is moved to ExitSpawn, camera returned, menu closed, and the pad returns to Idle. The timeout gives the feedback "Party creation timed out"; Cancel and leaving give none.
   * When the host presses Create, the server validates the selection (see Create validation). If invalid, it rejects with feedback and the pad stays Configuring. If valid, the server records map, difficulty, max players, and visibility on the pad, sets Boarding, stops the CreationTimer, and starts the BoardingTimer at BoardingTimeout. The host's client swaps the Create Party Menu for the Queue Menu in host mode and the billboard shows the configuration.
* Boarding
   * Other players touch JoinCollision. The server runs the join checks. On pass, the character is seated at the next free PlayerSpawn, the camera moved, the player added to the party, the count updated on the billboard, and the Queue Menu opened in member mode.
   * When the party reaches max players, the BoardingTimer is set to CapacityTime (a short auto-launch countdown).
   * Leaving: the Exit button in the Queue Menu, or disconnecting (a character that leaves its seat for any other reason, such as dying, also counts as leaving). The character goes to ExitSpawn and the party and billboard update. If a full party drops below full, the timer does not go back up; it keeps counting from where it is (a full party that loses someone in the last two seconds still launches).
   * If the host leaves during Boarding and other players remain, the earliest-joined remaining player becomes host, their Queue Menu switches to host mode, and they get the feedback "You are now the party host". If nobody remains, the pad returns to Idle.
* Launching. Any of these launches:
   * The host presses Launch in the Queue Menu
   * The BoardingTimer reaches zero
   * The CapacityTime countdown reaches zero after the party filled
   * A party of one launches solo. This is allowed and expected.
* On launch:
   * The pad is set to Departing and the billboard shows "Departing". Every client (party or not) sees the helicopter take off: it climbs 100 studs, then flies 1000 studs straight ahead, then disappears; a fresh helicopter is already on the pad by then. For party members the Queue Menu fades out while the camera stays on CameraPositionPart, and 2 seconds later the Departure Loading Screen comes in over the takeoff.
   * Party members' characters are moved to random spawns inside HoldingArea.Spawns. This removes their avatars from view in case of teleport issues.
   * All party members are teleported together (one TeleportAsync call with the player list, never per player) to a fresh reserved gameplay server. Join data: map, difficulty, playerCount (the number actually teleported, not max players; the gameplay server scales from this and never waits for more), hostUserId, padIndex for logging, and memberUserIds. Only players in that party can enter that gameplay server: the reserved server keeps outsiders out, and the gameplay server's arrival gate kicks anyone whose join data is missing or does not list them.
   * A failed teleport is retried (3 attempts, 2 s apart); on final failure the player respawns in SpawnZone with a "Teleport failed" message and their camera and menus are restored. A player who disconnects during Departing is simply excluded from the teleport. A player still in the lobby 10 s after a successful teleport call is treated as a failed teleport.
   * A fresh helicopter model is cloned from ReplicatedStorage, placed at HelicopterSpawn, and parented to the pad model. The pad is cleared and returns to Idle.
* Create validation (server, when the host presses Create). Reject with feedback if any fail:
   * The map and difficulty exist in the party config
   * The host has completed the map's requirement (if any)
   * The host has completed the difficulty's requirement on the selected map (if any)
   * 1 <= max players <= MaxPartySize
   * Visibility is Public or Friends Only
   * The pad is still Configuring and this player is still its host
   * The client greys out locked options, but the server is authoritative. Never trust the client's claim that something is unlocked.
* Join checks (server, on JoinCollision touch during Boarding). Reject with the given feedback if any fail:
   * Pad is Boarding: "Pad is being set up" / "Pad is departing"
   * Party has room: "Party is full"
   * Player not already in a party: "You're already in a party"
   * If Friends Only, the joiner is a friend of the host: "This party is friends only"
   * Joiners are not checked against map or difficulty unlocks. Access follows the host: a player who hasn't unlocked Frostbite Pass can join a Frostbite Pass party.
* Progression on completion (gameplay server): a run's completion of (map, difficulty) is recorded for a player only if that player had already completed its prerequisite themselves. A player carried through Nightmare by a friend does not unlock Nightmare. The gameplay server enforces this with the same requirement table the lobby uses.
* Every Helicopter Pad will be a Model, located inside Workspace.HelicopterPads, with a PadIndex attribute (1–6). Inside each model will be:
   * HelicopterModel
      * In the HelicopterModel will be a PlayerSpawns folder of invisible Seat instances (Seat1–Seat6) that party members are seated in. The seats are kept Disabled so the engine never auto-seats a passer-by; the server enables a seat only for its own Sit call and disables it again once it empties.
      * A Rotar model with a Rotar hub part; the blades are welded to the hub and the client spins it about world up.
   * HelicopterSpawn: An invisible part used to place newly spawned HelicopterModels
   * CameraPositionPart: Invisible part that will be used to position the player's camera
   * GuiPart.PadGui: GuiPart is an invisible part that PadGui (BillboardGui) will be parented to. PadGui will display all the necessary info.
   * JoinCollision: An invisible part, that when touched by a player character starts or joins a party if available
   * ExitSpawn: an invisible part outside the helicopter pad, where players leaving the party will be moved to
   * Other decorations (not needed by code)
   * Runtime pad state (state, host, map, difficulty, max players, visibility, current player count, departure time) is server Lua replicated as attributes on the pad model. The party member list itself stays on the server; only the count replicates. The pad model carries no authored map, difficulty, or max players values.
* Party config (one GameData module, nothing hardcoded in pad scripts). I need to be able to easily change:
   * CreationTimeout (default 30 s): how long a host has to press Create
   * BoardingTimeout (default 20 s): seconds after Create before auto-launch
   * CapacityTime (default 3 s): countdown once the party is full
   * MaxPartySize (default 6): upper bound on the players stepper
   * DefaultMaxPlayers (default 4) and DefaultVisibility (default Public): the menu's starting values
   * Maps, in carousel order, each with a display name, thumbnail, and an optional requirement (a map/difficulty completion). At launch: Deadman's Canyon (always available), Frostbite Pass (requires Deadman's Canyon on Normal).
   * Difficulties, in carousel order, each with an optional requirement evaluated against the currently selected map. At launch: Normal (always), Hard (requires Normal), Nightmare (requires Hard).
   * The config is validated at server start (unique ids, requirements reference real maps and difficulties, timeouts positive, defaults in range), failing loudly with the offending key named.
3D Store
* In addition to the Store Menu, there will be a physical store present in the lobby showcasing featured items and giving players another entry point for the store menu. 
* Inside Workspace.Store.Items will be a series of “showcase” models
   * Each has a StoreItem (string) attribute, listing the product associated with that model
   * Each StoreItem string will be associated with a StoreConfig file that lists each product by StoreName and contains all info needed for that product. 
   * Each model will have a part called Prompt with a Proximity Prompt child. 
      * When the proximity prompt is triggered:
         * If the StoreItem is a Robux purchase, it should instantly trigger the marketplace purchase flow for that item
         * If the StoreItem is an in-game currency purchase, it should open the store menu to that item 
Event Area
* This section of the map will be reserved for future events
* The idea is to have one place that showcases event rewards and allows players to check progress
* At launch the area will be closed off with a “coming soon” sign
Holding Area
* This is an empty room under the map, meant to hold player characters during the teleport process so teleports don’t get clogged when teleports fail or take a long time (see Helicopter Pads section) 
* Workspace.HoldingArea.Spawns:GetChildren() will return a list of spawn points for characters


Lobby Menus
HUD
* The HUD will show the player state and have buttons to access the various other menus
   * Player State/Balance 
      * Cash Balance (the game’s permanent currency)
         * Displays the user’s current Cash balance
         * When pressed, opens the store menu to the currency section
         * After the initial value is loaded, if the value increases the text should turn green and play the CoinsAdded sound and update the value. If the value decreases, the text should turn red and it should play the SaleSuccess sound before updating the value (cash was spent). 
      * Loadout Slots
         * Shows the players current load out slots 
            * 1 Weapon Slot
            * 1 Post Slot
            * 3 Defense Slots
   * Menu Buttons
      * Store Menu Button
         * When pressed, opens the store menu to the featured section
      * Loadout Menu Button
         * When pressed, opens the loadout menu
      * Daily Rewards Menu Button
         * When pressed, opens the Daily Rewards Menu
* When any menu (Store, Daily Rewards, Loadout, Create Party, Queue) is opened, the HUD should hide off screen 


Store Menu
* The store menu can be accessed from the Store Button in the HUD or via a proximity prompt in the 3D Store
* The Store Menu will have 5 different sections:
   * Featured
   * Weapons
   * Posts
   * Placements
   * Currency
* The menu will have 3 panels:
   * Section Tabs:
      * On the left side of the menu
      * A vertical row of buttons for each section. 
      * When a button is pressed, the inventory jumps to that section of the store. 
   * Inventory Panel: 
      * Shows all items in the store in one scrolling frame, that scrolls up/down vertically. 
      * The section tab for the section currently being viewed is highlighted. 
      * Every item should display a name, an image, and a price (either Cash or Robux)
      * When pressed, an item appears in the Item Panel
   * Item Panel: 
      * The Item Panel is on the right side of the screen and shows item details/buy option
      * When an item is selected in the inventory panel, the Item Panel is populated with that item’s data
      * It shows the item’s displayName, its image, a brief description, and buy button that says “BUY” along with the price in the appropriate currency (cash or Robux)
      * If a player presses the buy button:
         * If it is for Cash and the player has the currency, the cost should be deducted from their account and the item should be awarded
         * If it is for Cash and they don’t have the required currency, find the minimum currency pack needed to complete the purchase, and show the Robux purchase prompt for that item along with a Feedback message “You don’t have enough Cash”
         * If it is Robux, open the marketplace purchase prompt
* If the player owns an item and that item is not a repeatable purchase, that items should not be shown to that player, so only items they can actually buy are shown in the store.
* Everything else can be dealt with in the Loadout Menu
* The store menu should have a high level config file where items can be easily added and maintained. It should store all the information needed to generate a store listing for each item and complete the purchase process. That way there is only one file where all store data is added/maintained. 


Loadout Menu
* This menu allows players to view their current inventory and select their loadout
* Players have a total of 5 slots per loadout
   * 1 for a handheld weapon
   * 1 for a Post 
   * 3 for defenses
* This menu should follow a similar format as the store
   * Sections Tabs on the left
      * Buttons for each category (Weapons, Posts, Defenses)
      * When the button is pressed it jumps to that section in the inventory panel
   * Inventory in the middle
      * Show all loadout items owned and not owned, divided into sections (Weapons, Posts, Defenses)
      * One big scrolling frame that scrolls vertically 
      * Equipped items have a green square with a white checkmark in the top right corner of that item card
      * Unowned items have a red square with a white lock icon in the top right corner of that item card
      * Unequipped items that are owned have nothing in the top right
      * When an item card is pressed, it appears in the Selected Item Panel
   * Selected Item panel on the right + current equipped loadout
      * Like the store, when an item is selected from the inventory panel, its data populates the Selected Item Panel on the right side of the menu
      * It shows the item display name, its icon, a brief description and available actions for that item
      * If it is an owned item there should be a button to “Equip” if it’s not currently equipped and “Unequip” if it is currently equipped. 
      * If it is an unowned item, there should be a button to “Buy” with the cost on the buy button
         * If the player buys in this menu, the actions buttons update to “Equip”
         * Like the store, if they don’t have enough currency the proper currency dev product purchase should be prompted
         * If it is a robux purchase, the robux prompt should automatically be prompted after pressing buy 
      * If the player equips an item when their slots are all full, the existing item is replaced. If there are multiple items of the same type already equipped (and full), then the last item in that set is replaced. 
      * At the bottom of the selection panel, there should be 5 slots displaying the player’s current loadout
         * If you tap a filled slot, it should bring that item to the Selected Item Panel
         * There should also be text under the slots explaining what goes there
            * Weapon (under 1 slot)
            * Post (under 1 slot)
            * Defenses (under 3 slots)


Create Party Menu
* Shown to the host while their pad is Configuring (see Helicopter Pads section). Modelled on Dead Rails' Create Party screen.
* Layout, top to bottom:
   * Title: "Create Party"
   * Map carousel: left/right arrows around a large thumbnail with the map name overlaid. Locked maps are still shown (desaturated, lock icon, requirement text beneath such as "Complete Deadman's Canyon on Normal") but cannot be selected.
   * Difficulty carousel: same pattern, smaller. Locked difficulties greyed with lock icon and requirement text. Re-evaluated whenever the map changes; if the current difficulty is locked on the new map, the selection resets to the first unlocked difficulty.
   * Players stepper: "Players" label, − and + buttons, current value. Range 1 to MaxPartySize, starts at DefaultMaxPlayers.
   * Visibility toggle: one toggle labelled "Friends Only". Off = Public. Starts at DefaultVisibility.
   * Countdown text: "Cancelling in N seconds", red, updates every second.
   * Create button: disabled while the selected map or difficulty is locked.
   * Cancel button, directly beneath Create: behaves exactly like the creation timeout (host to ExitSpawn, pad to Idle), with no feedback.
* Opening the menu hides the HUD, like the other menus, and hides the pad billboards.
* Cancel, the timeout, and disconnecting are the only ways out of party creation; jumping out of the seat is disabled.
* All selection state is client-side until Create is pressed. The server receives one message with the full selection and validates it.
Queue Menu
* The Queue Menu appears when a player is in a Boarding party (see Helicopter Pads section)
* Every member sees: map name, difficulty name, "Players: X / Y", visibility ("Public" or "Friends Only"), the host's display name, and "Departing in N seconds"
* It has an ExitButton at the bottom center of the screen
   * When pressed, the player is removed from the party and their character is moved to the ExitSpawn part of the HelicopterPad model they're on
* The host additionally sees a Launch button, which launches immediately. When a member is promoted to host, their menu switches to host mode and shows it.
Daily Rewards Menu
* This menu can only be accessed from the DailyRewardsButton in the HUD
* It displays all the daily login rewards by day and the players current progress
* Copy the format used in Lucky Lights
* The designer should be able to easily the number of days and rewards for each day


Player Feedback Text
* This will be a simple ScreenGui containing a frame called FeedbackBox, with a TextLabel child. 
* There should be a remote event in ReplicatedStorage.Events called “GivePlayerFeedback” and a bindable event called “GivePlayerFeebackLocal”
* The server can fire the remote event (or the client can call the bindable event) with the below variables
   * Player
   * Message
   * TimeDisplay
* The PlayerFeedback gui listens for the events, takes the message, puts in TextLabel.Text, enables the gui, waits the TimeDisplay amount, and then disables the gui
* It’s an easy system to route player feedback through for all systems
Arrival Loading Screen
* This loading screen is shown when players first enter the experience. It covers the whole screen, including a phone's notch and home-bar safe areas (2026-09-16)
* It will have a title image and a loading bar
* The loading bar should fill as assets are loaded/data is set up
* Once loading is finished, the arrival loading screen should tween off screen
* A player arriving by teleport keeps the Departure Loading Screen they left with (handed through the teleport), so no Roblox loading screen shows and the same screen carries straight into the arrival load; its bar stays empty through the flight and fills once on arrival.
Departure Loading Screen
* This loading screen is shown when players are teleporting to the gameplay server
* Once the helicopter takes off from the helicopter pad with the local player, this loading screen should appear (2 seconds after takeoff, so the player sees the helicopter lift). For now it reuses the Arrival Loading Screen GUI with the subtext "Flying to <map>..." (decided 2026-09-11; may get its own art later)
* It will have a title image, subtext, and a spinning icon in the bottom right corner
* It will match the initial load screen of the gameplay server, so the teleport isn’t super obvious (it should just look like a menu flicker) 




Gameplay
Gameplay Overview
* All gameplay in the gameplay server is in first person (lobby is default third person). Admin dev tools free the mouse while a dev window is open.
* The core game consists of repeating the same core loop until the required uranium has been mined or until all players are dead or the mine grinder is destroyed 
* During the day (5 minutes), players harvest resources, upgrade abilities, and place defenses
* Players earn resources by mining raw ore and bringing it to the refinery to be refined into currency (Iron Bars, Gold Bars, Diamonds, and Uranium)
* The deeper players get into the mine, the harder the rock becomes and the more valuable the ore becomes
* After being refined, players can spend their currency on upgrading their mining abilities, upgrading their defense posts, buying/placing defenses, or repairing existing damage
* Gold is used on personal items/upgrades, Iron is used on shared resources, and Diamonds are a collective currency used to upgrade the camp level
* Upgrading the camp level unlocks mining upgrades and post upgrades (decided 2026-09-15: the mine itself is never locked; depth is gated by rock hardness alone)
* At night, waves of enemies spawn outside the camp, bent on destruction 
* Players must use their weapons, posts, and defenses to kill the horde before the horde destroys the refinery or kills all the players
* Waves get progressively harder every night, forcing players to think through their defenses and decide whether its better to improve mining speed or defense strength
* Waves spawn over 90 seconds whenever a day ends and lasts until all enemies have been killed, at which point a new day begins
* To win, players must collect the specified amount of Uranium they’ll be able to call the Apocalyptic Mining Co. rescue helicopter
* The monsters will give one final, climatic wave as the rescue helicopter arrives.
* Once safely inside the helicopter, the player has “won”


Maps
* The goal is to continue adding new maps, with fresh layouts, different challenges, new enemies, and obstacles
* At launch the goal is to have 2 different maps
   * Deadman’s Canyon: A desert mine in an old west canyon. This is the starter map, with a base only exposed on one side (so enemies only come from one direction). 
   * Frostbite Pass: An arctic mining facility with brutal weather, more exposure, and more intense enemies
* Each map will have its own separate mine spawn and enemy waves data, so they can be balanced differently. 
Map authoring (decided 2026-09-12)
* All maps live in the one gameplay place. Each map is a Folder under ServerStorage/Maps named by its Enums.Map key (DEADMANS_CANYON, FROSTBITE_PASS). Folders, not Models, so clicking a part in the Explorer selects only that part; things that should move as one unit (the helicopter pad) stay Models inside the folder.
* At run start the server clones the chosen map folder into Workspace as Camp. Only the active map exists in memory. Its scenery is static geometry and is the one place server-side parts are the right call.
* Every map folder carries the same required named parts, validated at startup with the map’s name in every message: HelicopterPad (with HelicopterSpawn and Spawns 1–6), Mine.Entrance (an anchored part whose position is the mine origin and whose look direction is the mine facing, snapped to the nearest axis), Refinery (a Model whose anchored Prompt part carries the refine ProximityPrompt), Walls (Wall1 to Wall6) and Posts (Post1 to Post6) folders of numbered Models plus a Gate Model (the fortification markers), Workbench (a Model whose anchored Prompt part carries the donate ProximityPrompt), and later store spots.
* Editing a map: run scripts/studio/map_tools.luau in Edit mode with ACTION = "load" to move the map folder into Workspace, edit, then run it with ACTION = "save" to move it back. Pressing Play with a map folder still in Workspace uses it as-is with a warning, so an unsaved edit still plays and is never lost. With the map loaded, ACTION = "preview_mine" generates its mine from Mine.Entrance exactly as the server would and draws translucent labelled boxes (bedrock envelope, the entrance tunnel, each zone, each hand-placed rock) into Workspace/MinePreview so the camp can be built around the real footprint; rerun it after moving the entrance, ACTION = "clear_preview" removes it, "save" removes it, and the server deletes any copy left behind at run start so it never shows in Play.
* Terrain is optional per map. A map that wants terrain adds a TerrainBounds part sized around it; “save” snapshots the terrain inside the bounds into a TerrainRegion in the map folder and “load” pastes it back. A map without TerrainBounds is a plain folder and the tools skip terrain. Terrain lives in the place file only.
* What a map cannot change: rock definitions, the weight kernel, block size, difficulty and party scaling, and the generator. Balance stays comparable between maps.
* A new map is: one Enums.Map entry with a public name, one row in the pad’s map carousel (thumbnail and unlock requirement), one mine file in GameData/Mines plus its registry entry, one wave file in GameData/Waves, and one folder under ServerStorage/Maps.
* Studio has no teleport data, so a dev tool starts a run as any map and difficulty without leaving Play.
Difficulty
* Every map will also have 3 difficulty levels: Normal, Hard, and Nightmare
* Difficulty is driven by the enemy waves. Harder rock is more swings per block, which is grind, not challenge; waves are decisions, positioning, and spending. Levers:
   * Enemy wave composition, count, HP, and damage (the main lever)
   * Ore scarcity: fewer veins on Hard and Nightmare, so upgrade choices bite (see Randomized Mine Creation §7). Uranium is exempt
   * Rock hardness never changes with difficulty by default (the knob exists but is off); mine layout never changes with difficulty or player count
Party scaling
* Parties are 1 to 6 players and the count can drop mid-run. The mine grid is fixed at generation and never resized; everything that scales with party size is applied at the moment of effect (a rock breaking, a wave spawning) from the current player count, so drop-outs adjust the run with nothing to migrate.
* Levers live in one PartyScaling table keyed by player count 1–6 (GameData/Control/PartyScaling), so 3 players can be tuned separately from 5:
   * Wave count and enemy HP multipliers. The main lever: a bigger crew digs faster and attracts bigger nights.
   * Uranium rescue target multiplier, kept modest (about 1.0 solo to 1.25 at six). Every mine must author enough uranium for the largest party; the generator validates supply against target × the six-player multiplier.
   * Per-player ore yield multiplier, applied only to Iron and Gold drops because those balances are per player. Diamonds and Uranium are shared and never scaled, so the mine’s authored totals stay their reference.
   * Rock HP is never scaled by party size. A crew of six clears rock faster, and the bigger night that follows is the balance.
* Multipliers are sublinear: six players are not six times as effective.
* The count is sampled once per day cycle at dawn, never continuously, so no wave changes under a player. The day-start message states it plainly (“Crew of 4. Waves 2.0x”).
* The scaling count only ever goes down after a run starts. A player leaving eases the run at the next dawn; nothing ever gets harder mid-run.
Resources
* The game has 3 Resource Types
   * In-Run Raw Resources
   * In-Run Refined Resources
   * Meta Currency 


Raw Resources
These are resources harvested directly from mining. They are not useful until they are refined into In-Run Currency via the Refinery. 


* Iron Ore: Dropped from breaking an Iron Block. Refined into Iron Bars. 
* Gold Ore: Dropped from breaking an Iron Block. Refined into Gold Bars. 
* Crystals: Dropped from breaking a Crystal Block. Refined into Diamonds.
* Uranium Ore: Dropped from breaking a Uranium Block. Refined into Uranium. 
Refined Resources
These are resources that have been refined from Raw Resources via the Refinery and can be used by the player. 


* Iron Bars: Refined from Iron Ore. Goes into a private player balance, but is spent on shared items like wall repairs and defenses. 
* Gold Bars: Refined from Gold Ore. Goes into a private player balance and is used on personal player upgrades (mining attributes, post upgrades, weapons)
* Diamonds: Refined from Crystals. These are a shared resource between all players, used to upgrade the Camp Level. 
* Uranium: Refine from Uranium Ore. This is the win condition. Getting X amount of Uranium allows players to call in a rescue helicopter. 
Meta Currency
These currencies survive a run and are used to buy permanent unlocks/upgrades/etc from the lobby store


* Cash: Earned by reaching specific milestones in a run or by purchasing with Robux
Camp Locations
Every camp consists of a few primary structures, each with a different purpose:
Mines
* Every camp has a mine: one open volume of rock behind a short entrance tunnel, sealed in bedrock
* Players can dig in any direction from the entrance; there are no walls, gates, or set paths inside
* The mine is authored as depth zones: the deeper a zone, the harder its rock and the more valuable its ore and uranium
* Pre-carved pockets (small caverns with ore on their walls) are scattered through every zone, so digging finds things and a crew has reasons to call each other over




Refinery
* Located just outside the mine entrance is the Refinery
* Players bring their raw materials here to get refined
* It functions like a simulator game, players walk up to to refinery, press a proximity prompt attached to the refinery that says “Refine Raw Resources” and all the raw resources are sucked from the player into the Refinery, which spits out the appropriate refined materials, which go straight into the player balance for spending (unless it’s diamonds, in which case it gets added to the Camp Level progress bar). 
* The Refinery is the focal point of the camp, as it displays the camp level and if destroyed, ends the game
* Players must protect the refinery at all costs
Mining Supplies Store
* This is where players upgrade their mining attributes and can buy consumable Mining Tools
* It will have a 3D representation of each mining attribute, showing the next level item for that attribute, that players can buy by triggering a proximity prompt for each model:
   * Pick Axe: Determines mining strength (how long to break a block)
   * Boots: Determines how fast a player moves
   * Backpack: How much raw resources a player is able to carry
* It will also have a 3D representation of each consumable Mining Tool that can also be purchased by activating a proximity prompt:
   * Torch: Creates light for dark mining caverns
   * Dynamite: Destroys a large chunk of rock when activated
* As built (M21, decided 2026-09-15): the store is Camp.MiningStore with one "<Item>Stand" Model per item (Pick Axe, Boots, Backpack, Jet Pack, Torch, Dynamite), each with a Base slab carrying a SurfaceGui (name, Gold price with icon, and a "Camp Level N required" line shown in red only while unmet) and an ItemSpawn part where the next level's model turns at 45 degrees per second. Every purchase is a ProximityPrompt held for one second, heard by the server directly; prices per level and consumable bundles live in GameData/Store/MiningSupplies (placeholders: Pick Axe 15/40/100/250, Boots 10/25/60/150, Backpack 10/30/80/200, Jet Pack 20/50/120/300 Gold for levels 2 to 5; Torch 5 Gold for four, Dynamite 12 Gold for one). Stands are drawn by each client from a catalogue published once as GameMetadata, because the next level differs per player; a locked stand disables its prompt locally, a maxed track reads "MAXED OUT" and keeps its top model on display. Gold Bars are spent from the refinery balance; the StorePurchase sound confirms a buy
Defense Store
* This is where players can buy consumable Traps
* Traps are consumable items that can be purchased with Iron and placed down outside the base. Once used, they disappear. They don’t directly attack enemies, but help corral, slow, or injure them when triggered. 
* Defenses are units brought in via the player’s load out – these are automated weapons that attack enemies in the traditional TD way.
* Traps are purchased just like Mining Tools in the Mining Store. They are 3D representations of each available trap in the Defense Store. Players can walk up to each and purchase via Proximity Prompt. 
Walls
* Along the perimeter of the base is a series of walls connecting 6 “Definsive Posts”
* Each Wall has its own Health
* As enemies attack, they can break a wall and enter the base
* Walls can be repaired and upgraded with Iron (see Wall Repair)
* As built (M23): 6 wall segments per map, each with its own HP. A destroyed segment turns to a low heap of rubble that players and enemies can walk over, and stays rubble until it is fully rebuilt
* The map's Wall, Gate, and Post models are placement markers: at run start the server swaps each for the current level's template (GameAssets/Structures: WALL_n, GATE_n, POST_n, plus WALL_RUBBLE and GATE_RUBBLE) at the marker's pivot, so a look is authored once and reused on every map
Defensive Posts
* There are 6 available Posts in each map (1 for each player, up to 6). 
* Players claim up to 1 post and spawn their post weapon (taken from their loadout)
* Post weapons are the most powerful weapons in the game and require a player to man them
* Players will have to man their post each night and attack the horde to keep them away
* Posts read as part of the wall, so their look follows the fortification level (POST_1 to POST_3). Each post carries a sign the server numbers from its place along the wall: "DEFENSE POST #1" to "#6"
Gate
* There is a gate that enters the camp
* It has its own HP and can be upgraded/repaired like walls
* During the day it opens to let players out into the Placement Area and closes at night
* The doors simply stand open all day and shut all night (decided 2026-09-17, replacing doors that opened for a nearby player). Players caught outside at night jet pack over the wall. A destroyed gate is rubble with no doors
* Authoring: each gate template has a GateDoor model with two leaves, DoorLeft and DoorRight, authored shut, and a marker part per leaf (DoorLeftOpen, DoorRightOpen) showing its open pose. Clients slide the leaves between the two; the server only decides whether they block
Placement Area
* This is a predefined area outside of the base, beyond the gates where players can place Traps and Defensive Units
* When in the Placement Area, available Traps and Defensive Units appear in a tool bar at the bottom of the screen
* Players tap one of them to bring up that Trap/Defensive Unit and a ghost version of that item appears in front of them, green if it can be placed in the current place and red if it cannot be placed. 
* Players can then place an item, at which time it is removed from their inventory (or paid for if it is a Defensive Unit). 
Helicopter Pad
* An empty Helicopter Pad made for the company rescue helicopter to drop off/pick up players
* This is where players spawn in after being teleported to the gameplay server. The model is Workspace.Camp.HelicopterPad with a HelicopterSpawn part and a Spawns folder of parts named 1-6; players are placed on them in arrival order (first to arrive on 1).
* On server start the company helicopter (ReplicatedStorage.GameAssets.Helicopter, the same template as the lobby) is spawned at HelicopterSpawn with its rotor already turning and the Helicopter sound looping on it as a 3D emitter for as long as the rotor turns (decided 2026-09-16; it stops with the blades or when the model is destroyed). Once every party member has loaded in (or 45 s after the first arrival), it waits 3 s and takes off exactly like a lobby departure.
* If players make it to the end, this is where the rescue helicopter lands
Tools
Tools are items that allow players to perform an action when equipped. Players load in with a weapon, a pick axe, and a hammer as their primary tools, but can buy other tools from the in-run stores. 
Tool Bar System
* The tool bar is a custom tool bar that mimics the functionality of the standard Roblox tool bar, but displays more than 3 items on mobile and keeps item order intact. 
* When a player has a tool, it appears in their tool bar at the bottom center of the screen
* When a player taps the tool in the tool bar, that tool is equipped
* Only one tool can be equipped at a time
* Tapping another tool or tapping the currently equipped tool in the Tool Bar unequips the currently tool 
* Keyboard: keys 1-9 toggle the slot with that number. Gamepad: the bumpers step through the bar; stepping past either end unequips. The number hint on each slot is shown only when a keyboard is present
* The bar shows what the server says the player holds (tool slots and the equipped tool are server state replicated as attributes); every tap is a request. The bar slides away while a menu is open
* The held model is a real Roblox Tool (decided 2026-09-15, as in the reference games): the server builds it from the GameAssets/Tools template when a tool is equipped and destroys it when not, so the Humanoid grips it, plays the arm pose and the forward swing on every use, and keeps it visible in first person. The Tool is only the visual; slots and counts stay in server Lua state and it is never stored in the Roblox Backpack (a Roblox-side unequip counts as an unequip request). Roblox's own hotbar is switched off
* Consumables (Torch, Dynamite) show how many are held as an "xN" badge on their slot and leave the bar when the last one is used
Pick Axe (Default Tool)
* Every player starts with a basic Pick Axe Tool by default
* Equipping a Pick Axe puts a player in Mining Mode, allowing them to mine Rocks in the mine 
* If a pick axe is not equipped, they cannot mine
* Players can upgrade their pick axe in the Mining Store, at which point the pick axe model and Tool Icon change to reflect the upgrade (as built: one held model per level, "PICK_AXE_<level>" under GameAssets/Tools, rebuilt in the hand on upgrade)
* Pick Axes do not perform any other function than mining (they cannot be used as a weapon)
Hammer (Default Tool)
* Every player also starts every run with a Hammer (decided 2026-09-17)
* Equipping the Hammer puts the player in Build Mode, the way the Pick Axe is Mining Mode: the reticle appears and an HP bar floats over every repairable structure (the 6 walls, the gate, the Refinery). Without the Hammer none of that shows
* Looking at a structure within reach highlights it, like a rock in Mining Mode, and the price of a swing appears under its HP bar as an amount, the Iron Bar icon, and the HP gained
* A press swings once and holding repeats, exactly like mining, on every input type (click, gamepad trigger, the touch action button, or a resting touch). See Wall Repair for what a swing does
Weapon (Default Tool)
* Every player also starts every run with a weapon
* Players can choose which weapon they bring on a run via the loadout menu in the lobby
* The default weapon is a Rifle
* Equipping the Weapon puts the player in Weapon Mode, allowing them to use the weapon to fight enemies
* This is helpful for early enemies and any monsters that get into the base or spawn in the mines
Mining Tools
* These are tools purchased from the Mining Store to help mining
* Includes Dynamite and a torch at launch
* Once purchased, these tools are added to the tool bar (after Pick Axe and Weapon)
* Tapping the tool icons for these equips the item and allows players to place/throw them at the desired spot
* Torch (as built, M21): with a torch held, a tap places one on whatever surface the reticle points at within pick axe reach, like Minecraft. The server spends one, keeps the torch as an invisible part clients dress with a stick, a flame, and a shadowless point light (24 studs), and remembers which mine block it sits on: when that block is mined or blasted, the torch drops as a pickup anyone can walk over. Placement plays TorchPlace
* Dynamite (as built, M21, decided 2026-09-15): with dynamite held, a tap throws a stick about ten studs ahead (GameData/Mining/Dynamite). The thrower's client simulates it, since only clients have the mine's blocks to land on; the server reads where it ended up (refusing a blast further than 60 studs from the throw) and after a 3 second fuse clears every breakable rock within one block of it (a 3x3x3 cube), broadcasting the breaks like swings. All ore from a blast drops on the ground; none goes to a backpack. The fuse hiss rides on the stick and the bang plays where it went off, both 3D so every player hears them in place; clients draw sparks, smoke, and a flash
* Sounds for both are authored in SoundService.Sfx like every other sound; the server clones them onto objects with an AudioEmitter (SpatialSoundManager), and each client keeps an AudioListener on its camera
Mining
Mining is half the game and is the only way players can win a run. However the goal is to keep mining simple, lightly strategic, but tactilely rewarding. Every mine is randomly generated based on a set of parameters created for each map/difficulty level. We want mining to feel exactly like mining in Roblox, so players just intuitively know how to mine. 


Rocks
Every mine consists of a grid of cube Rocks (4x4 Studs). Every Rock will use the same 4x4 cube mesh, but will have a different texture based on the Rock Type. Every Rock mesh will be located in ReplicatedStorage/GameAssets/Rocks (the central asset folder) named by its RockName enum key (IRON_3, STONE_1, BEDROCK); the player-facing name (“Iron 3”) is the enum’s public name. Clients clone from here; the server never holds rock parts.  
Rock Types
There are three types of Rock: Filler Rock, Resource Rocks, and Bedrock.  
Filler Rock
These Rocks do not give any resources, but fill empty space and slow progress. The vast majority of the mine will be filled with Filler Rocks. All Filler Rocks are breakable, but some are harder to break than others:


* One filler rock, Stone, in five tiers (decided 2026-09-15: several filler kinds did the same job and made ore harder to spot). Same mesh, darker as the tier rises
* Stone 1 (10 HP)
* Stone 2 (20 HP)
* Stone 3 (40 HP)
* Stone 4 (80 HP)
* Stone 5 (160 HP)
Resource Rocks
These Rocks give any raw resources when destroyed. More valuable Resource Rocks appear more often in deeper zones. Resource Rocks also have multiple tiers, with higher tiers taking longer to break but giving more resources: 


* Iron
   * Iron 1 (15 HP, 3 Iron Ore)
   * Iron 2 (30 HP, 10 Iron Ore)
   * Iron 3 (60 HP, 29 Iron Ore)
   * Iron 4 (110 HP, 78 Iron Ore) 
* Gold 
   * Gold 1 (20 HP, 3 Gold Ore)
   * Gold 2 (40 HP, 10 Gold Ore)
   * Gold 3 (75 HP, 30 Gold Ore)
   * Gold 4 (140 HP, 87 Gold Ore) 
* Crystal
   * Crystal 1 (30 HP, 2 Crystals)
   * Crystal 2 (60 HP, 5 Crystals)
   * Crystal 3 (110 HP, 14 Crystals)
   * Crystal 4 (200 HP, 39 Crystals) 
* Uranium (100 HP, 1 Uranium Ore)


Bedrock
Bedrock is a Rock that cannot be destroyed by anything, but is used to define the boundaries of the mine. Every mine will be fully surrounded by bedrock on all sides (except for the entrance), which ensures players cannot dig out into some unwanted area.  
Mine Layout
* The mine is one open, explorable volume. Decided 2026-09-15, replacing sealed chambers with camp-level gates: chambers made mining linear and killed the “look what I found over here, help me” moment, which needs room to split up and things to find
* Depth zones: the mine file is an ordered list of zones running back into the mountain, and each zone sets how hard the rock is, which ore tiers dominate, how much ore and uranium it holds, and how many pockets it has. Rock gets harder and ore gets better the deeper a crew digs
* Soft gating: nothing in the mine is ever locked. A starting pick axe against deep rock is a wall the crew can feel without a wall being there, and the Camp Level gates the pick axe tiers in the Mining Supplies Store, so depth is still paced by Diamonds
* Pockets: pre-carved caverns of a few blocks, with ore exposed on their walls, scattered through every zone. They are what digging discovers and what a crew talks about
* The entrance: a short tunnel through the front bedrock into the first zone, the only opening in the shell. Past it the crew chooses its own direction
* Clear signal without walls: rock colour and material change with tier, so deeper rock looks different, and the camp level unlocks in the stores are the goal to work toward
* There are 4 zones per mine, though the system needs to be built in a way that this is a customizable number
* This is to encourage players to upgrade their mining equipment as the game progresses


Mining Rock
* To mine Rock, players must first equip their Pick Axe – which puts them in “Mining Mode”
* Mining is intended to feel just like Minecraft, so it is intuitive to a broad amount of players
* There needs to be a small dot in the center screen, representing the player’s aim
* With Pick Axed equipped, any rock in the center of the players screen is highlighted (Highlight, white borders, transparent fill)  and the name of that Rock is shown to the right of the player’s central reticle over a health bar; no numbers (decided 2026-09-15, as in Minecraft and Dome Keeper: the block and the bar are the readout). The bar turns red once the block is crumbling; bedrock shows no bar  
* To start breaking the highlighted rock players:
   * Mobile (as built 2026-09-16, modelled on Minecraft Bedrock's split controls): the screen is also the camera, so a bare tap never swings. A finger that rests still in the free screen area for a quarter second starts a hold on the block under the reticle; dragging that finger afterwards turns the camera while the hold keeps mining, and lifting it ends the hold. A touch-only action button beside the jump button (labelled with the equipped tool until tool icons exist) does the same on press and hold, and is the only touch input for the torch (press places) and dynamite (press throws)
   * PC: LMB click to hit once, hold LMB to hit continuously 
   * GamePad: RT to hit once, hold RT to hit continuously 
* A held input swings every 0.45 s (hitIntervalSec in GameData/Mining/PickAxe). The reticle reaches 12 studs, the 3-block pickaxe reach the mine generator assumes. After the held block breaks, the hold waits until the reticle has rested on the next block for one interval before swinging again, so every first hit on a block is intended (decided 2026-09-16); a fresh press skips the wait
* Every swing is a request (TryHitRock with the block's grid cell). The server refuses it unless the pick axe is equipped, the cell is a diggable rock, the player's root is within reach plus a 6-stud tolerance of the block centre, and 0.35 s have passed since that player's last accepted swing. Damage is the pick axe level's value; the server then tells every client the block's remaining HP (crack state) or that it broke
* Until M46 art, the crack overlay is a placeholder tint: the block darkens at 60% and again at 30% HP. Hit and break bursts are placeholder particles in the block's colour; the RockHit sound (filler) or ResourceHit sound (ore, metallic) plays on a hit and RockBreak on a break, for players within 60 studs
* Only the Rock currently highlighted will be hit 
* The Rock’s health bar is updated to reflect its current health after each hit
* There will be a particle effect with each hit (rock pieces flying) and noises to make it feel tactile and satisfying 
* Once the Rock’s HP is at or below 0, it breaks
* If any Ore is inside, it drops out (see Collecting Ore below) 
* Players can mine as much a they want, they just can’t collect any additional Ore if their Backpack is full
* A backpack icon with current status is always shown in the HUD (taken slots/total slots), on the right edge a little above mid height with the jet pack fuel bar directly under it (decided 2026-09-15; it was Mining Mode only). The balances panel sits on the left edge at the same height; neither may sit where the chat window, the top bar, the thumbstick, or the jump button can cover it (GUI rule, 2026-09-16). On touch the stack is pinned directly above the Jet Pack button. The Roblox player list is off in the gameplay place (2026-09-16)
* When the Backpack is full, the Backpack Icon/numbers should turn red and a small text label should say “Backpack full” (see “A Game About Digging a Hole” for reference), and the Fail sound plays once on the change to full
Collecting Ore
* Dropped resources are automatically given to the player who breaks the block. 
* Any resources that the breaker doesn’t have room for in their Backpack (raw resource inventory) is dropped on the ground
* Dropped Ore is automatically picked up by any player who walks over it with empty Backpack space (like Minecraft)
* When Ore is collected, it should make a pop sound, again like Minecraft
* Players can tap their Backpack icon to bring up a Backpack inventory menu
* This menu shows how much of each Ore is in their backpack (only showing Ore that the player actually has) 
* Capacity is counted in ore units, so the HUD’s taken/total is total ore over capacity (“12/15”). Contents, level, and capacity are server run state replicated as private attributes; the backpack HUD button is always shown and opens the contents menu as an overlay
* A broken resource rock gives the breaker its yield. Iron and Gold are multiplied by the party table (PartyScaling) and rounded half up; Crystal and Uranium never are. Party size is the current player count until M28 samples it at dawn
* Ore that does not fit lands by the block as a pickup (one invisible server part carrying resource and amount; clients draw a bobbing cube in the ore’s colour), scattered up to 1.5 studs and settled onto the ground beneath (inside the mine the server walks the grid column down to the next rock; outside it raycasts the camp), so drops never hang where the block was. Ore at the player's feet still flies into the HUD backpack from the nearest screen edge. Any player with room within 5 studs collects it, partial amounts included; the server checks four times a second. A pickup nobody takes disappears after 120 s, and past 300 pickups the oldest go first (GameData/Mining/Backpack)
Refining Ore
* Players refine Raw Resources by taking them to the Refinery
* The Refinery is the main camp building, located just outside the Mine Entrance
* It is a complicated looking machine, that grinds up Raw Resources and returns them as Refined Resources that can be spent
* The goal is to keep this very simple and recognizable to anyone who has played a Roblox simulator where you exchange resources for currency
* Players simply walk up to the Refinery, trigger a proximity prompt on it that says “Refine Raw Resources” 
* Triggering the proximity prompt begins the refining process
* All Raw Resources in the player’s Backpack are automatically refined at the same time
* Icons for each Raw Resource in the players backpack are sucked out of their backpack and into the Refinery mouth
* The Refinery spins and grinds the resources for a few seconds and spits out icons for each of the Refined Resources it generates, which should fly into the player’s HUD to the appropriate balance bar
* The balance bars should shake and update as each icon hits it
* This process should all feel very satisfying, both visually and audibly – lots of particle effects, sounds, etc
* As built (M20, decided 2026-09-14): every ore refines one to one (GameData/Refinery, adjustable per ore). The prompt is heard by the server directly, never as a client message; it empties the whole backpack, disables itself for a 2 second grind, then pays out. Iron Bars and Gold Bars are private per-player balances; Diamonds go into the shared Camp Level pool and Uranium into the shared rescue count, both shown to every player. The Uranium row reads “total / target”, the target being the mine’s solo uraniumTarget times the PartyScaling multiplier for the current crew (sampled at dawn from M28), and turns green once reached
* Placeholder sequence until Phase 9 polish: coloured discs (at most six per resource) fly from the backpack button into the Refinery on screen while RefineryGrind plays, then from the Refinery to each balance row (Diamonds to the Camp Level billboard above the Refinery) with RefineryPayout; a row bumps as each icon lands and its value updates when the last one does. The Refinery must be visible on screen for the icons to aim at it; otherwise they aim at the screen centre
* Dev tools: Fill backpack spreads all four ores evenly; Reset refinery clears every balance and the camp progress
Randomized Mine Creation


1. What a mine is
A mine is a single grid of 4×4×4 stud cube blocks, generated on the server when a run starts. It is never built by hand in Studio.
The grid is one open volume of rock inside a bedrock shell, entered through a short tunnel at the mouth. It is authored as zones: consecutive depth bands running back into the mountain, each setting the rock mix, ore, uranium, and pocket count for its band. Nothing inside the mine is walled or gated; the deeper rock is simply harder (decided 2026-09-15).
Pockets are small caverns pre-carved into each zone so ore is exposed on their walls and there is something to discover.
A designer authors roughly 40 numbers per mine. Difficulty is one more.
________________


2. Coordinate model
The grid has its own local space, always axis-aligned. A single module maps that space into the world, so the mine can sit anywhere and face any direction without the generator knowing about it.
Axis
	Meaning
	Direction
	Depth
	Into the mountain
	Grows away from the entrance
	Height
	Up
	Block 0 is the lowest
	Width
	Across
	Centred on the mine’s centreline
	* Origin is the centre of the entrance mouth, at floor level.
* Facing is one of +X, -X, +Z, -Z — the direction a player walks to enter. Restricted to 90° steps so blocks stay axis-aligned.
* The bedrock floor sits at origin height, so players can never dig below ground level. All mining is sideways and up.
All zones share the mine’s centreline and the floor. There is deliberately no per-zone lateral offset: a narrower or shorter zone simply gets thicker bedrock around it.
________________


3. Rock definitions
A single global table, authored once for the whole game. Mines never redefine these.
Filler rock
Fills space and slows progress. Yields nothing. Ordered by hardness — that order is load-bearing, see §6.
Index
	Rock
	HP
	1
	Stone 1
	10
	2
	Stone 2
	20
	3
	Stone 3
	40
	4
	Stone 4
	80
	5
	Stone 5
	160
	Resource rock
Yields raw ore when destroyed. Cluster size is a property of the rock, not of the mine — Iron 3 clusters the same way everywhere. Iron and Gold tiers 1 to 3 were shrunk to radius 0 to 1 (one to seven blocks) on 2026-09-15 after play showed them clumping; the table below still shows the original radii.
Rock
	Tier
	HP
	Yield
	Cluster radius
	Iron
	1
	15
	3 Iron Ore
	1–2
	Iron
	2
	30
	10 Iron Ore
	1–2
	Iron
	3
	60
	29 Iron Ore
	1–2
	Iron
	4
	110
	78 Iron Ore
	0–1
	Gold
	1
	20
	3 Gold Ore
	1–2
	Gold
	2
	40
	10 Gold Ore
	1–2
	Gold
	3
	75
	30 Gold Ore
	1–1
	Gold
	4
	140
	87 Gold Ore
	0–1
	Crystal
	1
	30
	2 Crystals
	1–1
	Crystal
	2
	60
	5 Crystals
	1–1
	Crystal
	3
	110
	14 Crystals
	0–1
	Crystal
	4
	200
	39 Crystals
	0–1
	Uranium
	—
	100
	1 Uranium Ore
	0–0
	Uranium has no tiers. It is treated as its own resource with a single definition, and tierBias does not apply to it.
Structural rock
Rock
	Behaviour
	Bedrock
	Indestructible. Forms every boundary
	________________


4. Mine-level parameters
Authored once per map. Difficulty does not appear here — see §7.
Parameter
	Type
	Default
	Notes
	mineId
	string
	—
	Which map this defines
	seed
	number or nil
	nil
	nil means fresh per run. Set to reproduce a layout. The seed actually used is logged at the start of every run so any playtest report can be reproduced
	origin
	Vector3
	from Mine.Entrance
	Centre of the entrance mouth, at floor level. Read from the map folder’s Mine.Entrance part, not authored in the file, so a designer moves the mine by dragging a part
	facing
	enum
	from Mine.Entrance
	+X / -X / +Z / -Z, the entrance part’s look direction snapped to an axis
	bedrockThickness
	number
	1
	Blocks, applied to every face and divider
	bedrockOverrides
	table
	empty
	Per-face exceptions (front, far, floor, ceiling, side). Rarely needed
	entranceWidth
	number
	3
	Blocks, centred on the centreline. The mouth
	entranceHeight
	number
	3
	Blocks up from the floor. Keep it within pickaxe reach unless climbing exists
	entranceDepth
	number
	3
	Blocks of tunnel carved into zone 1 past the front bedrock, so the crew can walk in and pick a direction
	veinEntranceClearance
	number
	0
	Blocks of rock between a generated vein and the entrance tunnel
	pocketRadiusMin
	number
	1
	Smallest pocket, as a cluster radius (0 is one block, 1 is 7, 2 is 33)
	pocketRadiusMax
	number
	2
	Largest pocket
	uraniumTarget
	number
	—
	Uranium rocks (each yields one ore) needed to call the rescue helicopter, solo. Party size scales it (see Party scaling)
	defaultZoneHeight
	number
	—
	Zones inherit this unless they say otherwise
	defaultZoneWidth
	number
	—
	Same
	veinEntranceClearance is a real design decision, not a technicality. At 0, resource rock can break the tunnel surface and a player reads the first zone’s worth the moment they walk in. Above 0, everything is discovery. Hand-placed veins (§9) ignore this.
________________


5. Zone parameters
Zones are authored as an ordered list, front to back. Zone N inherits every unspecified value from zone N−1; zone 1 inherits from the mine defaults. In practice a later zone is three or four lines.
Parameter
	Type
	Default
	Notes
	depth
	number
	inherited
	Blocks along the dig axis
	height
	number
	inherited
	Blocks up from the entrance floor line
	floorDrop
	number
	inherited (0)
	Blocks the zone extends below the entrance floor line (added 2026-09-15), so a mine goes down as well as up. The grid's floor row sits above the deepest drop; hand-placed rock heights are measured from the floor line
	width
	number
	inherited
	Blocks
	hardness
	1.0–5.0
	inherited
	Stone tier mix. See §6
	tierBias
	1.0–4.0
	inherited
	Which ore tiers dominate. See §6
	iron
	number
	inherited
	Vein clusters of iron in this zone
	gold
	number
	inherited
	Vein clusters of gold
	crystal
	number
	inherited
	Vein clusters of crystal
	uranium
	number
	inherited
	Vein clusters of uranium
	pockets
	number
	inherited (0)
	Pre-carved caverns in this zone. See §8
	handPlaced
	list
	empty
	Never inherited. See §9
	Derived, never authored
Value
	Rule
	Zone position in the grid
	Accumulated from earlier zone depths; zones abut with no divider
	Grid dimensions
	See §10
	Ore totals
	Reported by the generator, not authored
	If a designer can type a derived value directly, it will drift out of sync with the zone list. Keep them out of the data.
________________


6. The weight kernel
hardness and tierBias both compress a set of weights into one number, and they use the same function. Build it once.
weights(center, count, spread = 1.2):
    for i in 1..count:
        w[i] = max(0, 1 - abs(i - center) / spread)
    normalise w so it sums to 1
    return w
spread is a global tuning constant. It controls how much neighbouring categories bleed in. At spread = 1.2 (decided 2026-09-15; 1.5 put too much tier 2 next to the entrance):
hardness
	Stone 1
	Stone 2
	Stone 3
	Stone 4
	Stone 5
	1.0
	86%
	14%
	—
	—
	—
	2.0
	12%
	75%
	12%
	—
	—
	3.0
	—
	12%
	75%
	12%
	—
	4.0
	—
	—
	12%
	75%
	12%
	5.0
	—
	—
	—
	14%
	86%
	Note that spread = 1.2 with whole-number hardness means a zone is three quarters its own tier with an occasional block of each neighbour, so “the rock gets harder as you go deeper” reads zone by zone. Raising spread toward 1.5 blends neighbouring zones into each other. Tune it once, globally, after seeing a mine in-game.
Do not drop spread toward zero. At spread = 1.0 a whole-number hardness produces a zone of one single stone tier, which reads as flat and artificial. The bleed is what makes rock look natural.
tierBias works identically over the four ore tiers. A zone’s iron = 30 with tierBias = 2.0 places 30 iron clusters split 4 / 22 / 4 / 0 across tiers 1–4. Fractional splits use largest-remainder rounding so the tiers always sum to exactly the authored count; a tie goes to the lower tier.
Uranium ignores tierBias; its count is placed directly.
________________


7. Difficulty
Two numbers per difficulty, applied at generation time. Difficulty never changes layout — zone count, sizes, the entrance, and pockets are identical across difficulties. Difficulty lives mainly in the waves (see Difficulty under Maps); in the mine it only makes ore scarcer.
Difficulty
	Difficulty value
	Vein divisor
	Normal
	1.0
	1.0
	Hard
	1.4
	1.2
	Nightmare
	1.9
	1.4
	The difficulty value is the general scalar other systems (waves) read. The vein divisor is the mine’s own lever. Two effects:
effectiveHardness = clamp(
    hardness + (difficulty - 1) * hardnessShift, 1, stoneTierCount)
effectiveVeinCount = round(baseCount / veinDivisor)
hardnessShift is a global constant, default 0.0, so rock hardness does not change with difficulty. The knob exists (GameData/Control/Difficulty, alongside the difficulty values and vein divisors; spread lives in GameData/Rocks/Weights) for a map that deliberately wants grittier rock at Nightmare. round is half-up. Uranium is exempt from the vein divisor: its count is always as authored, so the target-to-supply ratio is a plain designer choice on every difficulty. At Nightmare, a zone authored with 30 iron veins generates 21.
Player count is not a generator input. The grid is identical for 1 or 6 players; party size scales waves, per-player ore yield, and the uranium target at the moment of effect (see Party scaling under Difficulty).
________________


8. The entrance and pockets
The entrance is the only opening in the bedrock shell: a tunnel entranceWidth blocks wide, centred on the centreline, entranceHeight blocks up from the floor, carved through the front bedrock and entranceDepth blocks into zone 1 so a crew can walk in and choose a direction.
* An even width cannot centre on a single block, so it leans one block toward positive width.
* Keep the height at or below pickaxe reach (~3 blocks) unless climbing is a real mechanic, or the tunnel has a ceiling players can see but not touch. The generator warns.
* Past the tunnel a player faces solid rock in every direction. That is the point: from here on the crew makes its own tunnels.
Pockets are pre-carved caverns: per zone, pockets discrete spheres of air with a radius drawn between pocketRadiusMin and pocketRadiusMax, dropped at random inside the zone. They are carved before veins, so ore lands around them and never inside them, and every pocket wall is a discovery. A pocket a player breaks into is the “look what I found” moment the open mine exists for, so a zone with no pockets is a uniform block that only ever gets tunnelled.
Pocket count is the strongest single lever on how a zone plays: a few big pockets read as caverns, many small ones as honeycomb. Zone height and pocket count together decide how much of a zone is actually reachable from a standing start; the report shows both.
________________


9. Hand-placed rock
Optional per zone, usually empty, never inherited.
Field
	Notes
	rock
	Type and tier, e.g. Crystal 3
	depth
	Blocks from the zone’s front face
	height
	Blocks up from the floor
	widthOffset
	Blocks from the centreline. Negative is left
	cluster
	Radius override. Optional
	Hand-placed rock is stamped after generated veins and pockets and overwrites both. It ignores veinEntranceClearance.
This exists for one reason worth protecting: a high-tier vein deliberately placed in shallow reach, a few blocks past the mouth, that a starting pickaxe can barely dent. That block sells upgrades better than any UI, and no richness number will reliably produce it. Keep the escape hatch.
________________


10. Generation pipeline
Ordered. The order is load-bearing and each step explains why.
1. Resolve difficulty. Apply §7 to produce effective hardness and vein counts.
2. Resolve inheritance. Walk the zone list, filling unspecified values from the previous zone, then from mine defaults.
3. Validate. See §11. Fail loudly before spending time generating.
4. Size the grid.
gridDepth  = frontBedrock + sum(zoneDepths) + farBedrock
gridHeight = max(zoneHeights) + floorBedrock + ceilingBedrock
gridWidth  = max(zoneWidths) + sideBedrock * 2
5. Fill the entire grid with bedrock. Everything is solid until carved. This is what makes “sealed on all sides” the default state rather than a rule to enforce. A zone narrower or shorter than its neighbour simply has thicker bedrock around it, with no special handling.
6. Fill each zone with filler rock. Roll each block of the zone’s volume against the §6 hardness weights. Zones abut, so no bedrock ever stands between them.
7. Carve pockets. Per zone, pockets spheres of air at random positions inside the zone, radius between pocketRadiusMin and pocketRadiusMax. Before veins, so ore lands around pockets and never inside them.
8. Stamp generated veins. Per zone, per resource: split the count across tiers by tierBias, place each cluster at a random rock position inside the zone, respecting veinEntranceClearance and skipping pocket air. Uranium is stamped last and gets two guarantees ordinary ore does not, because it is the win condition: every uranium rock sits within pickaxe reach of the floor, so it is minable from a standing start with no step-carving, and no uranium rock lands on another, so placed always equals authored.
9. Stamp hand-placed rock. After generated veins and pockets, so it always wins.
10. Carve the entrance. After veins and hand-placed rock, so nothing can plug the mouth. It is the only opening in the shell.
11. Report. Log the seed, grid size, per-zone diggable volume and pocket volume, and derived ore totals per zone, both requested (vein counts × tier split × mean cluster size × yield) and actually placed, since overlapping clusters overwrite each other and silently lower the count. Also report how much of each zone’s diggable volume sits within pickaxe reach of the floor, so a zone that is mostly decoration is caught in the log rather than in playtest. Also report cumulative uranium per zone (“uranium so far”) so the designer can see the counter’s progression against uraniumTarget. The designer authors vein counts but cares about ore, so the generator has to close that loop.
After the report, check the grid itself: if fewer uranium rocks survived than the largest party needs, refuse the result and name the seed. Validation checks the authored numbers; this checks what was actually built, so no seed can ever ship unwinnable.
Randomness. One Random is seeded from the run seed, and each zone draws from its own sub-seed derived from the run seed and the zone index. Changing zone 2’s numbers therefore never reshuffles zone 1, so a designer sees only the effect of their edit.
________________


11. Validation
Every check must name the parameter and the zone, so a misconfiguration is identifiable without reading code.
Fatal — refuse to generate:
* The authored uranium total is below the map’s uraniumTarget × the largest party multiplier. The run is unwinnable for a full crew
* A zone has a dimension below 1, or a negative pocket count
* entranceWidth or entranceHeight exceeds zone 1, or entranceDepth exceeds zone 1’s depth. The mouth would cut bedrock
* pocketRadiusMin is negative or pocketRadiusMax is below it
* facing is not one of the four permitted values
Warn and continue:
* entranceHeight is above pickaxe reach with no climbing mechanic enabled
* A hand-placed entry falls inside the entrance tunnel. It will be carved away
* A hand-placed entry falls outside its zone bounds
* Pockets and the entrance leave a zone with no diggable volume
* hardness outside 1.0–5.0 (one per stone tier) or tierBias outside 1.0–4.0 (one per ore tier). Clamp
________________


12. Runtime
Camp level. Reaching a camp level unlocks store tiers, not mine volume (M22). It is still the pacing beat of a run and should not happen silently — it needs an audible and visible moment for the whole crew, wherever they are.
Damage. Rock HP is tracked server-side by grid position, not on the rendered block. Blocks are pooled and streamed, so a part that showed a half-mined block may be recycled or unloaded; damage keyed to position survives both.
Visual damage states. Three states per the GDD: untouched, 60%, 30%. Author these as a crack overlay applied on top of any rock, not as separate textures per rock type. Seventeen rock types × 3 states is 51 assets to author and maintain; a shared overlay is 2, and it works automatically on rock types added later.
Designer tooling. A dev tool regenerates the mine in a running session, with an optional seed, without restarting the run. This is the tuning loop: edit the mine file, sync, regenerate, walk in. A pure expected-ore preview computes the report’s requested-ore and HP totals from the mine file alone, so a designer can read the balance sheet of an edit before generating anything.
Open questions, to revisit only on real feedback:
* Independent per-block filler rolls may read as static noise rather than strata. If so, roll per 2×2×2 cell or bias toward a neighbour’s rock. Visual only; balance is untouched.
* Fixed layout may feel same-shaped after many runs. Pockets already vary per seed; if the envelope itself needs to, add bounded jitter to zone depths rather than freer generation, so balance stays authored.
________________


13. Worked example
A four-zone mine. Note how short zones 2–4 are — everything unspecified is inherited.
return {
    mineId = "canyon_default",
    seed = nil,
    origin = Vector3.new(-359.259, -6, -20.911),
    facing = "-X",

    bedrockThickness = 1,
    entranceWidth = 3,
    entranceHeight = 3,
    entranceDepth = 3,
    veinEntranceClearance = 0,
    pocketRadiusMin = 1,
    pocketRadiusMax = 2,
    uraniumTarget = 20,

    defaultZoneHeight = 8,
    defaultZoneWidth = 24,

    zones = {
        {   -- 1: soft, generous, everything reachable
            depth = 20,
            hardness = 1.0,
            tierBias = 1.0,
            iron = 34, gold = 12, crystal = 2, uranium = 2,
            pockets = 2,

            handPlaced = {
                -- The upgrade advertisement. A few
                -- blocks past the mouth, barely dentable
                -- with a starting pickaxe.
                { rock = "Crystal 3", depth = 6,
                  height = 1, widthOffset = 2 },
            },
        },

        {   -- 2: mixed rock, better ore
            depth = 24,
            hardness = 2.0,
            tierBias = 2.0,
            iron = 30, gold = 18, crystal = 6, uranium = 4,
            pockets = 3,
        },

        {   -- 3: taller, harder
            depth = 24,
            height = 12,
            floorDrop = 6,
            hardness = 3.0,
            tierBias = 3.0,
            iron = 22, gold = 22, crystal = 12, uranium = 8,
            pockets = 4,
        },

        {   -- 4: cathedral, the deepest and richest rock
            depth = 28,
            height = 16,
            floorDrop = 8,
            hardness = 4.0,
            tierBias = 4.0,
            iron = 16, gold = 20, crystal = 18, uranium = 14,
            pockets = 5,
        },
    },
}
Uranium is spread 2 / 4 / 8 / 14 so the counter moves from the first zone. Nothing stops a crew tunnelling straight for zone 4, except that its rock is hardness 4.0 against a level 1 pick axe; that curve, and the camp level gating the pick axe tiers, is the pacing.
As of 2026-09-15 the live file (GameData/Mines/DeadmansCanyon) also drops each zone below the entrance floor (floorDrop 2 / 4 / 6 / 8) and evens out zones 1 and 2 to 22 iron / 20 gold and 22 / 22, because Gold feeds the store now while Iron's sinks (walls, traps) arrive later.
Darkness: the mine is lit by the client, not the sun. From the mouth to 100 studs in, each client blends the sun, ambient, and skybox light down to near black by how far along the mine's forward axis its character stands (ClientMineLightingManager), so torches are what light the deep mine and walking out never shows a step.
At Nightmare (vein divisor 1.4), the same file generates zone 2 with the same rock, 21 iron, 13 gold, and 4 crystal clusters, and unchanged uranium. No second file, no overrides.
Mining Attributes


Pick Axe
* A player’s Pick Axe determines how much HP is removed from a Rock per hit
* There are 5 Pick Axe Levels (though this should be easily adjustable). Level 2 is a plain Gold purchase; from there, level N+1 requires Camp Level N (decided 2026-09-15), so the first upgrade teaches the store before the camp level matters
* Damage per hit by level: 5 / 12 / 25 / 50 / 100 (GameData/Mining/PickAxe, DAMAGE_BY_LEVEL; the list length is the level count; level 5 is a placeholder until tuned). Level 1 breaks Stone 1 in two swings and needs 22 for Iron 4, which is what sells the upgrade. Everyone is level 1 until the Mining Supplies Store (M21), where levels 2 to 5 cost 15 / 40 / 100 / 250 Gold (placeholders)




Backpack
* Capacity by level, in ore units: 15 / 25 / 40 / 60 / 90 (GameData/Mining/Backpack, CAPACITY_BY_LEVEL; the list length is the level count; decided 2026-09-15 to start small so refining trips are frequent; tune in play). Five levels, level N+1 needing Camp Level N, like the pick axe. Everyone is level 1 until the Mining Supplies Store (M21), where levels 2 to 5 cost 10 / 30 / 80 / 200 Gold (placeholders)
Boots
* The second mining attribute, bought and upgraded in the Mining Supplies Store like the others: five levels, level 2 a plain Gold buy, level N+1 needing Camp Level N. Boots only do one thing: walk speed (decided 2026-09-15)
* Walk speed by level, in studs per second: 16 / 18 / 20 / 22 / 24 (GameData/Mining/Boots, WALK_SPEED_BY_LEVEL; the list length is the level count; placeholders until tuned in play). Level 1 is Roblox’s default, so a new player moves exactly as they expect
* The level is run state like the pick axe level, held by the server and applied to the character’s Humanoid on upgrade and on every respawn, so nothing about speed is ever trusted from the client
* Boots change no other stat: no jump, no fall damage, no carry. Those belong to the Jet Pack and the Backpack
* Store prices for levels 2 to 5: 10 / 25 / 60 / 150 Gold (placeholders)
Jet Pack
* The fourth mining attribute (added 2026-09-15), bought and upgraded in the Mining Supplies Store like Pick Axe, Boots, and Backpack: five levels, level 2 a plain Gold buy, level N+1 needing Camp Level N. It is the climbing mechanic the mine generator has been waiting for (IS_CLIMBING_ENABLED), so mines can go taller and more vertical, and a player at the bottom of a shaft they dug has a way out
* Controls, kept to one gesture. Keyboard and gamepad: double-tap Jump and keep holding it; the jet pack burns while Jump is held and stops the moment it is released. Touch: a Jet Pack button sits directly above Roblox's jump button, the same size as it, placed against the live jump button at runtime so it can never overlap it, with the tool action button directly to the jump button's left (2026-09-16); holding it burns, releasing it stops. No hover, no descend input, no toggle state to remember: burn is up, release is down
* Fuel is the whole design. The tank starts at 1.5 seconds of burn and each upgrade adds half a second (level 5 holds 3.5 seconds); both numbers are tunable in GameData/Mining/JetPack (retuned 2026-09-15 from 3 s plus 1 s, which cleared the mine at level 2). Fuel drains only while burning and the pack cuts out when the tank is empty, so the player falls. It refills whenever the pack is not burning, anywhere, at half a second of fuel per second, and a tank run dry locks the pack until it is full again (decided 2026-09-15, replacing an on-the-ground rule that felt inconsistent); stopping early keeps the remaining fuel. That lock is what keeps a hop from becoming flight
* Burning lifts the player straight up at a fixed climb speed (12 studs per second, so a level 1 burn lifts about four blocks and level 5 about ten, under the 72-stud mine) while the normal walk controls still steer, so a hop can be aimed onto a ledge
* The tank shows as a bar with a jet pack icon directly under the backpack button on the right edge of the HUD, always visible (decided 2026-09-15): green while the pack answers, red while it is locked and refilling. The Fail sound plays the moment the tank runs dry
* No restrictions on when it can be used (decided 2026-09-15): in the mine, in the camp, during combat, at night. The fuel limit is the only guard against abuse; if a hop over the wall turns out to be a problem in play, the fix is tuning fuel, not adding rules. The mine’s bedrock and the map’s own edges are the only bounds. Fall damage rules are decided with Revives (M32); until then the cut-out height must stay survivable
* Server authority follows the tools rule: the client asks to start and stop burning, the server owns the fuel and refuses a start on an empty or locked tank; the owning client applies the lift itself (it owns its character's physics) from the replicated burning flag. Every player sees the pack on the character’s back (GameAssets/Wearables/JET_PACK) with a flame and light while it burns, and hears the burn as a 3D loop on that character. Store prices for levels 2 to 5: 20 / 50 / 120 / 300 Gold (placeholders)
Posts
Defenses
Traps
Wall Repair
* Repair works like mining (decided 2026-09-17, replacing a single full-price repair prompt): in Build Mode every Hammer swing at a damaged structure adds 10 HP and costs the player 4 Iron Bars. The last swing pays only for the HP still missing, rounded up. Swings repeat every 0.45 seconds while held, and the Hammer reaches 17 studs, 5 more than the pick axe, because a wall is big
* Every swing is a request the server validates (Hammer equipped, structure damaged, within reach of the structure's bounds, cooldown, enough Iron). A swing that lands plays the hammer sound for everyone nearby. With too little Iron the Hammer does not swing and the player is told once per press
* A destroyed wall or gate is rubble and stays rubble while it is rebuilt swing by swing from zero; its bar reads "Rebuilding n / max". It stands back up only when fully rebuilt (GameData REBUILD_STAND_SHARE = 1, decided 2026-09-17: a wall that reappears after one swing could be flickered in front of the horde for 4 Iron a time). Rebuilding a level 1 wall is 20 swings and 80 Iron
* Repair is never capped by Camp Level
* Fortification level: the gate, all 6 walls, and the look of the posts share one level, 1 to 3 (decided 2026-09-17: a half-wood, half-concrete wall reads as a bug). HP by level: Wall 200 / 400 / 800, Gate 300 / 600 / 1200. An upgrade keeps each structure's share of HP rather than healing it, and rubble stays rubble
* The Workbench: a bench just inside the gate with a floating sign like the Camp Level one, showing "FORTIFICATIONS LEVEL n", a bar of the crew's donated Iron ("pool / required", Iron Bar icon), and a red "Camp Level N required" line while the next level is locked. Its proximity prompt, "Donate Iron", moves 10 of the player's Iron Bars into the shared pool per press; no Hammer needed. When the pool reaches the price (150 Iron for level 2, 400 for level 3) the fortifications upgrade by themselves, and every player gets the CampLevelUp sound and "Fortifications upgraded to level n" in the blue announcement box. Level N needs Camp Level N, and donations are refused while locked so nobody's Iron is stranded in a pool that cannot complete
* The Refinery is repaired with the Hammer like a wall but never bought: its HP level is the Camp Level (1000 / 1500 / 2200 / 3000), so Diamonds toughen it. Refinery destroyed is the loss condition hook for the run state machine (M28)
* All numbers live in GameData/Camp/Structures and GameData/Camp/Level; placeholders until enemies exist to tune against
Defense design notes (2026-09-16, source for M23 to M27; sections above are updated as each lands)
* S9 — Manned posts (4). Limit 1–2 each. Never auto-fire. The only heavy damage in the game
   * Gatling: balanced generalist, sustained fire. Weakness: never excellent, poor vs Brute/Titan
   * Cannon: heavy single target. Weakness: slow reload, useless vs swarms
   * Mortar: arcing, large blast radius, reaches canyon mouth. Weakness: minimum range, no air
   * Flak Cannon: airburst, hits Flyer groups. Weakness: cannot hit ground
   * Upgrade structure: 4 stat lines (damage, rate, range, one specific), 4 tiers each; tier 1 is the base post. Then one exclusive branch fork with a capstone, gated at Camp Level 4, paid with gold
   * Claiming: one player owns a post — their type, their money, their upgrades. Free to leave mid-fight. Claims release on disconnect, post retains built tier
   * Launch unlocked: Gatling, Cannon
* S10 — Auto turrets (6). Cheap, weak, numerous. Limit 4–8 each. Loadout: pick 3 from the unlocked roster in the lobby, locked for the run
   * Sentry Gun: single target, steady. Workhorse
   * Flamethrower: short range, wide cone, burn DoT
   * AA Turret: air only, cannot hit ground
   * Frost Tower: radius slow, occasional freeze. No damage
   * Tesla Coil: chain lightning, brief stun
   * Sniper Tower: long range, high damage, slow. Answers Launchers
   * Tuning rule (non-negotiable): a full turret field holds night 4 and is overrun by night 8. Turrets buy time and cover flanks. They never win
   * Launch unlocked: Sentry, Flamethrower
* S11 — Placements (6). Bought freely in-run at the Defense Building. No loadout — all available always. Reactive, not committed
   * Tiger Teeth: barrier (HP). Blocks movement, enemies attack it
   * Spike Trap: trap (recharge). Pops on contact, damage, resets. No slow
   * Goo Pit: trap (persistent). Slows crossers. No damage
   * Land Mine: one use. Proximity, big damage
   * Bear Trap: one use. Stun + damage, single target
   * Decoy: barrier (HP). Draws attacks away from the wall
   * Barriers have HP. Traps have reset times. No expiry timers, no wear-by-use
   * Destroyed placements are gone. Rebuy with gold — a resource cost, not a death spiral
   * Hard rule: enemies must always be able to reach the wall. If all routes are blocked, they attack the nearest barrier. This prevents maze-building
   * Launch unlocked: Tiger Teeth, Spike Trap, Land Mine
* S12 — Handheld weapons (4)
   * Rifle: default. Useful forever, never sufficient
   * Shotgun: close range. Runners, breaches
   * Sniper: long range. Launchers, Brutes
   * Flamethrower: short range, wide, DoT. Packed Grunts
   * 2 in-run tiers each: damage, reload. Ammo unlimited
Camp Level
* The Camp Level is shared by the whole crew and starts at level 1. Diamonds from the Refinery fill it like an XP bar: the display shows “CAMP LEVEL n” over a bar reading “pool / needed”, and a full bar rolls over into the next level with the remainder carried, several levels at once if one payout is big enough. At the top level the pool reads MAX
* What a level unlocks (decided 2026-09-15, table settled 2026-09-16): the next tier of every upgrade track, in both stores. Pick Axe, Boots, Backpack, and Jet Pack in the Mining Supplies Store have five levels each and level N+1 requires Camp Level N, so level 2 is always a plain Gold buy: players learn mining upgrades before the camp level ever matters. Posts have four tiers per stat line; tier 1 is the base post with no upgrades, and tier N requires Camp Level N, so the first post upgrade is a level 2 goal. A post's exclusive branch fork and its capstone need Camp Level 4, the top level. Weapon in-run tiers are Gold only and never gated. Which posts, turrets, placements, and weapons a player owns at all is a lobby Cash matter, never Camp Level. Diamonds are the only collective currency, so the collective decision of when to level the camp is what paces both halves of the loop: how deep the crew can dig by day, and how hard the posts hit by night
* Fortifications (M23): the shared level of the walls, gate, and posts has three levels, and level N requires Camp Level N, the same rule as the posts; the table is STRUCTURE_TIER_REQUIRED_CAMP_LEVEL_BY_TIER in GameData/Camp/Level. The Refinery's own HP level is the Camp Level itself
* Never capped: consumables (Torch, Dynamite), Traps, Defensive Units, and wall repair. Those are priced by Gold or Iron alone so the stores never feel closed
* Nothing in the mine is ever locked by Camp Level. Depth is paced by rock hardness against the pick axe tier the camp level allows (see Mine Layout)
* A store item above the cap still shows, with its price greyed, its prompt disabled, and a red “Camp Level N required” line under the price, so the goal is always visible; the line disappears once the level is met
* Display: the bar and level live on a billboard above the Refinery, the camp’s focal point, not in the HUD; the info is only needed while refining, and the top of the screen is kept for the day clock and wave state. Diamonds fly from the Refinery into the billboard bar during a payout
* Level up is the pacing beat of a run and never happens silently (decided 2026-09-16): every player, wherever they are, hears the CampLevelUp sound and gets the notification “Camp upgraded to level n” in the blue announcement box at the top centre of the screen (the bottom feedback box is too easy to miss under the action; decided 2026-09-16); nothing to dismiss, no list of unlocks, no menu. The Refinery billboard rolls over. A payout that crosses several levels at once notifies once, for the level reached
* Diamonds needed per level live in GameData/Camp/Level (placeholder pacing 20, 80, 250 until tuned in play, so the top level is 4), and so does the required-camp-level table per upgrade track (M22): the stores read the table, never a formula, so lifting or loosening a cap is a one-line change
* The Camp Level is run state: it starts at 1 every run and is never saved to a player
Wave Cycle
Monsters
Random Events
Adjusting for Different Player Counts
Revives


Spectate Mode
Menus
Audio


Design Requirements
* All numbers that affect gameplay must be easily accessible and not hard coded, so that a designer can easily adjust them to balance gameplay
* All art that is added to the workspace through gameplay/code (mine rocks, monsters, weapons, tools, posts etc) needs to be pulled from a central location so that artists/designers can easily replace/adjust one instance (for example, mine Rocks “templates” are in ReplicatedStorage and are cloned when generating a mine)
Technical Overview
Product/Marketing targets
This game should play well on phones with pretty low bandwidth and even only 2GB of RAM. The biggest limitation will probably be bandwidth if we do things the naive roblox way, so keep this in mind (and see ‘shadow’ system below). 
Reference Games/Code
Review the code in ~/md (md stands for ‘merge dragons’, though the game ended up being called ‘fuse dragons’, sometimes referenced as ‘dragons’), and ~/square (‘lucky squares’, ended up being called ‘lucky lights’, or just ‘lights’). Dragons was written almost entirely by hand and code quality and structure is considered quite good. Lights was entirely written with AI, and while the structure is good, the code has largely not been reviewed.


There are many systems or structures we should reuse from these games. Lights is newer than dragons, and hence tends to be more advanced and have better/deeper abstractions. Systems we should definitely copy–the player state loading/saving system, which uses a 3rd party library (code in both projects) for user profiles that handles errors incredibly well; the loading/ready system for players in Lights is excellent. Using Iris library for dev tools, and locking dev tool access to the members of a group, is also a great setup we should use here too. Lights has a sophisticated system to capture screenshots and turn them into image assets in Roblox Studio that we likely want to copy.


Both games separate out most “dials” or otherwise configurable numbers controlling game play into data-only scripts in the ServerScripts/GameData folder. Let’s do the same here. Difference–in the other games, that folder was NOT checked into git, and editing the files in studio was authoritative. In this game, let’s check them into git.


Both games also have an object/item system which works well, but is a bit less applicable here where less is persistent between sessions. Analyze and make suggestions on this.


Copy and use coding standards from those games to start. Don’t use abbreviations in variable names, use luau type system a lot (but the LSP is not great, so don’t overindex here–we want to use colon functions for object ‘method’ functions for example, and the LSP doesn’t handle it well). Setup a test suite you can run from Studio for all core, testable units, same as Lights.


Look to Lights for some key decisions–all messages go over one ‘RemoteEvent’ (and are then dispatched by name) to be able to detect missing handlers. Almost all game data setup is sent from the server to the client in a spot in the PlayerGui, which allows client-inspection of all of a client’s state by a human to be trivial, which helps us quickly debug problems.


Key thing about Roblox development–we don’t want to use parts/models/meshes in the data model on the server very much, because they get replicated to clients at very high fidelity, and very high cost, giving us very little control. Look at the ‘shadow’ system used in both games–we place an invisible part on the server, clients get it (or not) depending on streaming, and then we set attributes on that part on the server, that the clients read and then render changes, animations, etc. This game is a bit different from our reference games, so there will be differences here, but while we must validate all significant game play actions on the server, we should be doing that by keeping game state in the server’s lua data structures, mostly NOT in parts in the workspace (except for shadows).


In particular, mining needs to be carefully planned and thought through–the server probably should NOT store every minable thing as parts, but instead as a lua data structure that gets shared with the clients, with messages sent back and forth with updates to that data structure.
Decisions/Project Philosophy
We are designing this game to be commercially successful. We want a good amount of consistency–let’s decide font choices for UI and standards for centering, capitalization, when to use exclamation points (very rarely), etc. and then stick with them across the project. When copying in big parts of UI from our reference games (especially the premium shop, inventory, index, playtime rewards, daily rewards, etc.), always adjust the UI to our standards, even if the functionality remains the same or at least similar.


We want to relatively quickly build up the core game play, even if the polish and graphic fidelity doesn’t exist (we can play the game where every attacker is just a cube just fine), and then add artwork and polish and sound effects etc. later.


Our first step is to take this document and have you turn it into a set of phases of development. Phase 1 is probably “get studio, rojo, etc. setup and the places able to be run”, phase 2 is probably “setup player joining/leaving and lobbies”, phase 3 might be mining, phase 4 might be full game play. There should probably be about 8-10 total phases, each with 3-10 milestones each representing one or a small set of features that we will be building in sequence. Don’t ever commit code or mark a milestone complete without explicit approval. As we setup this project and you’re copying lots of standards and memories and instructions from the reference projects, please ask us if you think anything is contradictory or if you think something is a bad idea or wouldn’t apply, or if you just don’t know if we want it in this project. Once we build a few milestones together, you’ll be able to make a lot of progress with a lot less feedback while still staying aligned to the goals of this project.